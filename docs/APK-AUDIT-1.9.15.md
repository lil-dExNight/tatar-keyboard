# APK-AUDIT-1.9.15 — аудит артефакта

Дата: 2026-09-05. Объект: `dist/tatar-keyboard-1.9.15.apk`.

## Артефакт

| | |
|---|---|
| Размер | **1 796 219 Б** (потолок 3 145 728 Б, запас 42,9 %) |
| SHA-256 | `7bfd3794e23f10dfa3b22515d063f2c072db621db85b70b4619cbbb0f5ba1b94` |
| Версия | 1.9.15 / versionCode 31 |
| Подпись | v2, `CN=Tatar Keyboard`, `98ca6feb…42ad` — тот же ключ всей линейки |
| Сборка | `scripts/release_pack.sh`; unsigned 1 816 951 Б, после zopfli −20 732 Б |
| Разрешения | ровно `android.permission.VIBRATE` |

**Воспроизводимость:** два полных прогона `release_pack.sh` → один SHA-256
`7bfd3794…`, `cmp` чистый.

## Что вошло

Разбор backlog'а error-prone (`docs/ERRORPRONE-TRIAGE.md`), 64 находки, закрыто 5:

* **настоящий латентный дефект** — `Settings.java` дереференсил результат
  `Bundle.getString` для ограничения `pref_keyboard_color`; значение не-строкового
  типа роняло **всю** загрузку управляемых политик с NPE, а не только эту
  настройку. Закрыто проверкой на null, запинено
  `AppRestrictionsSourceContractTest`;
* целочисленное деление в float-выражениях — подпись языка на пробеле смещалась
  до полупикселя;
* дублирующая ветка `TYPE_TEXT_VARIATION_FILTER`, неотличимая от общей;
* приватный помощник с именем `equals` рядом с настоящим `equals(Object)`;
* необъяснённый пустой `catch` (поведение верное, намерение записано).

Остальные 59 разобраны по классам с обоснованием; все 13 `ReferenceEquality`
проверены поимённо — везде намеренное сравнение по идентичности.

## Дельта к 1.9.14

Записей в архиве: **247 → 247**.

| Что | Дельта |
|---|---|
| `classes.dex` | +4 Б несжатого |
| `classes2.dex` | ±0 |
| `resources.arsc` | ±0 |
| `assets/dexopt/baseline.prof` | пересобран |
| `AndroidManifest.xml` | версия |
| APK целиком | **±0 Б** |

**Все записи `assets/` кроме `baseline.prof` байт-в-байт прежние** — оба словаря,
обе таблицы биграмм, все пять эмодзи-ассетов, три NOTICE. При обновлении с 1.9.14
ничего не переинфлируется. Ресурсы не изменились вовсе.

## Гейты

`scripts/release_check.sh` — **12/12 PASS**, OVERALL PASS:

| Проверка | Результат |
|---|---|
| `gates.gradle_test` | **1105 тестов в 120 файлах, 0 падений** |
| `gates.lint_release` | baseline без новых ошибок |
| `gates.python_tests` | 348 тестов в 12 файлах |
| `gates.no_internet` | оба уровня |
| `artifact.size` | 1 796 219 Б, запас 42,9 % |
| `artifact.asset_pins` | 16 значений по 4 ассетам совпали |
| `artifact.emoji_assets` | 5 файлов совпали с деревом |
| `artifact.permissions` | ровно `[VIBRATE]` |
| `artifact.signature` | `98ca6febfed6…` |
| `artifact.version` | 1.9.15 / 31 = `app/build.gradle` |
| `artifact.changelog` | `metadata/en-US/changelogs/31.txt` |
| `artifact.delta` | предыдущий 1.9.14 (vc 30), 0 Б (+0,0 %) |

error-prone: **64 → 59** предупреждений.

## Прогон

Смоук на эмуляторе `tt_suggest_a14` (Android 14, 1080×2280): APK ставится,
клавиатура поднимается в поле сообщения, раскладка рисуется корректно,
`logcat -b crash` по пакету пуст.

**На живом устройстве 1.9.15 не проверялась** — телефон отключён от хоста.
Правки этой версии в поведении видны только в корпоративных политиках и на
полпикселя в подписи пробела; предыдущая версия (1.9.14) на железе проверена.

## Открытое

* Замеры холодного старта и PSS относятся к 1.9.12.
* Локаль `tt` содержит витринные заметки только для 27–31.
* Не проверено человеком: TalkBack на слух, Direct Boot, MIUI-убийца процесса,
  ощущение набора, настоящий планшет.
