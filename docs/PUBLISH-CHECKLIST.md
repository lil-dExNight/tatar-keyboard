# PUBLISH-CHECKLIST — публикация v1.9.13

> **Перецелено 2026-09-04 на кандидата v1.9.13 / versionCode 29** — две починки
> по итогам первой проверки на живом устройстве (`docs/DEVICE-UAT-1.9.12.md`):
> Д-2 «нет Enter на планшетах» (`docs/TABLET-ENTER.md`) и Д-1 «панель эмодзи
> под панелью навигации» (`docs/EMOJI-PANEL-NAVBAR.md`). Текущий артефакт —
> `dist/tatar-keyboard-1.9.13.apk` (1 796 219 Б, SHA-256 `02ce15b3…`), аудит —
> `docs/APK-AUDIT-1.9.13.md`. Пометки про 1.9.12 и ранее ниже остаются историей;
> галочки и числа тех прогонов на 1.9.13 не переносятся.
>
> Различия кандидата 1.9.13, которые процедура обязана учесть: store-заметка —
> `metadata/en-US/changelogs/29.txt` (+ копии ru-RU и tt); **в релизе только код
> и два новых XML-ресурса** — все 13 записей `assets/` байт-в-байт прежние
> (сверено по CRC32), включая оба словаря, обе таблицы биграмм и все пять
> эмодзи-ассетов; изменён только `assets/dexopt/baseline.prof`. При обновлении с
> 1.9.12 на устройстве НИЧЕГО не переинфлируется — проверено живым прогоном на
> POCO C71: `firstInstallTime` не изменился, обе починки подтверждены на
> подписанном артефакте.
>
> Состояние на 2026-09-04: коммиты и тег `v1.9.13` **запушены** в
> `origin` (`lil-dExNight/tatar-keyboard`), CI на релизном коммите `d88b0d1d`
> **зелёный** (и на ветке, и на теге). Остаётся создать GitHub Release с APK
> строго из `dist/` и запрос в IzzyOnDroid.

> **Перецелено 2026-09-02 на кандидата v1.9.12 / versionCode 28** (финальный
> аудит `docs/FINAL-AUDIT-2026-09-02.md` закрыт: миссии 4 эмодзи-подсказок —
> `docs/emoji-suggest/FIXES.md` (tt-покрытие поиска 1318/1389, дефолт тоггла ON,
> конфузиблы, сброс поиска при скрытии), CODE-FIX (B4/C5/C6/C7), TOOLING-FIX
> (C1–C4), B3 (baseline-профиль 2 708 правил), META (витрина)). Текущий
> артефакт — `dist/tatar-keyboard-1.9.12.apk` (1 796 107 Б), аудит —
> `docs/APK-AUDIT-1.9.12.md`. Пометки про 1.9.11 и ранее ниже остаются историей;
> галочки и числа тех прогонов на 1.9.12 не переносятся. Различия кандидата
> 1.9.12, которые процедура обязана учесть: store-заметка —
> `metadata/en-US/changelogs/28.txt` (+ копии ru-RU и tt); **изменились два
> эмодзи-ассета** (`emoji_search_v1.txt` — tt-покрытие, `emoji_suggest_v1.txt` —
> tt-строки и конфузиблы) — они читаются прямо из APK, на устройстве ничего не
> переинфлируется; словари и таблицы биграмм байт-в-байт прежние (пины те же,
> проверено живым прогоном обновления на эмуляторе: `firstInstallTime` не
> изменился, имена файлов в device-protected storage идентичны); гейт
> `artifact.emoji_assets` теперь сверяет МНОЖЕСТВО файлов в обе стороны (C2).
> Публикация, push и теги — действия оператора.
>
> **Перецелено 2026-09-01 на кандидата v1.9.11 / versionCode 27** (миссия
> NEXTWORD-RACE: фикс первого NEXT_WORD-запроса — `docs/NEXTWORD-RACE.md`,
> дефекты D1+D2). Текущий артефакт — `dist/tatar-keyboard-1.9.11.apk`
> (1 792 011 Б), аудит — `docs/APK-AUDIT-1.9.11.md`. Пометки про 1.9.10 и ранее
> ниже остаются историей; галочки и числа тех прогонов на 1.9.11 не
> переносятся. Различия кандидата 1.9.11, которые процедура обязана учесть:
> store-заметка — `metadata/en-US/changelogs/27.txt`; **в релизе только код**
> (три файла правки D1/D2) — словари, таблицы биграмм и эмодзи-ассеты
> байт-в-байт прежние, при обновлении с 1.9.10 на устройстве НИЧЕГО не
> переинфлируется (проверено живым прогоном обновления на эмуляторе:
> `firstInstallTime` не изменился); гейт пинов `release_check.sh` теперь
> включает проверку `artifact.emoji_assets` (все 5 файлов `assets/emoji/`
> побайтно с деревом) — проверок стало 12. Публикация, push и теги — действия
> оператора.
>
> **Перецелено 2026-09-01 на кандидата v1.9.10 / versionCode 26** (план
> эмодзи-подсказок, все три миссии: `docs/EMOJI-SUGGEST-PLAN.md`,
> `docs/emoji-suggest/DATA.md`, `docs/emoji-suggest/ENGINE.md`). Текущий
> артефакт — `dist/tatar-keyboard-1.9.10.apk`, аудит —
> `docs/APK-AUDIT-1.9.10.md`. Пометки про 1.9.9 и ранее ниже остаются историей;
> галочки и числа тех прогонов на 1.9.10 не переносятся. Различия кандидата
> 1.9.10, которые процедура обязана учесть: store-заметка —
> `metadata/en-US/changelogs/26.txt`; **появился новый ассет
> `assets/emoji/emoji_suggest_v1.txt`** — он НЕ входит в гейт пинов
> `release_check.sh` (тот покрывает только 4 ассета словарей/биграмм); его пины
> охраняют python-набор `tests/emoji_suggest_pack/` и JVM-контракт
> `EmojiSuggestAssetTest.kt`, прогоняемые гейтами `gates.python_tests` и
> `gates.gradle_test` того же `release_check.sh`. Четыре пиненых ассета
> словарей/биграмм байт-в-байт прежние — на устройстве при обновлении с 1.9.9
> они НЕ переинфлируются (имена файлов не меняются), эмодзи-ассет читается
> напрямую из APK (проверено живым прогоном обновления на эмуляторе:
> `firstInstallTime` не изменился). Публикация, push и теги — действия оператора.
>
> **Перецелено 2026-09-01 на кандидата v1.9.9 / versionCode 25** (финал кампании
> ужатия, миссии SIZE-1/2/3: `docs/SIZE-SCHEMA2.md`, `docs/SIZE-SCHEMA3.md`,
> `docs/SIZE-CAMPAIGN.md`). Текущий артефакт —
> `dist/tatar-keyboard-1.9.9.apk` (1 775 548 Б), аудит —
> `docs/APK-AUDIT-1.9.9.md`. Пометки про 1.9.8 и ранее ниже остаются историей;
> галочки и числа тех прогонов на 1.9.9 не переносятся. Различия кандидата
> 1.9.9, которые процедура обязана учесть: store-заметка —
> `metadata/en-US/changelogs/25.txt`; **изменились все четыре пиненых ассета**
> (оба словаря — TATDICT schema 2, обе таблицы — TATBIGR schema 3 с кросс-
> референсом в словарь по raw SHA-256; пины перезакреплены, 16 пинов совпали в
> release_check); baseline-профиль регенерирован под новые читатели; **релизный
> пайплайн — `scripts/release_pack.sh`** (unsigned assembleRelease →
> zipalign -z → apksigner sign v2-only), а не голый gradle; воспроизводимость
> сохранена (два прогона полного пайплайна → один SHA-256). На устройстве при
> обновлении с 1.9.8 все четыре ассета переинфлируются ровно один раз (новые
> schema-id и raw SHA-256 в именах файлов — штатный путь, проверен живым
> прогоном обновления на эмуляторе: `firstInstallTime` не изменился).
> Публикация, push и теги — действия оператора.
>
> **Перецелено 2026-09-01 на кандидата v1.9.8 / versionCode 24** (миссии
> «разговорный корпус», части A и B: `docs/CORPUS-CONVERSATIONAL-RU.md` и
> `docs/CORPUS-CONVERSATIONAL-TT.md`). Текущий артефакт —
> `dist/tatar-keyboard-1.9.8.apk` (2 095 592 Б), аудит —
> `docs/APK-AUDIT-1.9.8.md`. Пометки про 1.9.7 и ранее ниже остаются историей;
> галочки и числа тех прогонов на 1.9.8 не переносятся. Различия кандидата
> 1.9.8, которые процедура обязана учесть: store-заметка —
> `metadata/en-US/changelogs/24.txt`; **изменились оба пиненых ассета таблиц
> биграмм** (`bigrams/russian_bigrams_v1.tatbigr.zlib` и
> `bigrams/tatar_bigrams_v1.tatbigr.zlib` — разговорное дообучение, пины
> перезакреплены, 16 пинов совпали в release_check); словари байт-в-байт
> прежние; кода в релизе нет (dex байт-в-байт прежний). На устройстве при
> обновлении с 1.9.7 обе таблицы распакуются заново ровно один раз (имена
> файлов несут новые raw SHA-256 — штатный путь, проверен живым прогоном
> обновления на эмуляторе: `firstInstallTime` не изменился, словари не
> переинфлировались). Публикация, push и теги — действия оператора.
>
> **Перецелено 2026-08-31 на кандидата v1.9.7 / versionCode 23** (миссия
> «русская таблица биграмм», `docs/RUSSIAN-BIGRAMS-REPACK.md`). Текущий
> артефакт — `dist/tatar-keyboard-1.9.7.apk` (2 098 856 Б), аудит —
> `docs/APK-AUDIT-1.9.7.md`. Пометки про 1.9.6 и ранее ниже остаются историей;
> галочки и числа тех прогонов на 1.9.7 не переносятся. Различия кандидата
> 1.9.7, которые процедура обязана учесть: store-заметка —
> `metadata/en-US/changelogs/23.txt`; **изменился пиненый ассет
> `bigrams/russian_bigrams_v1.tatbigr.zlib`** (перепаковка от текущего словаря,
> пины перезакреплены, 16 пинов совпали в release_check); кода в релизе нет
> (dex байт-в-байт прежний); на устройстве русская таблица распакуется заново
> один раз при первом показе русской раскладки (имя файла несёт новый raw
> SHA-256 — это штатный путь, не регрессия). Публикация, push и теги —
> действия оператора.
>
> **Перецелено 2026-08-31 на кандидата v1.9.6 / versionCode 22** (миссия
> «релиз-кандидат 1.9.6»: закрытие находок аудита `docs/AUDIT-2026-08-31.md`).
> Текущий артефакт — `dist/tatar-keyboard-1.9.6.apk` (2 105 064 Б), аудит —
> `docs/APK-AUDIT-1.9.6.md`. Пометки про 1.9.5 ниже остаются историей;
> галочки и числа тех прогонов на 1.9.6 не переносятся. Различия кандидата
> 1.9.6, которые процедура обязана учесть: store-заметка —
> `metadata/en-US/changelogs/22.txt`; пользовательские-видимые изменения —
> ₽ и «АБВ» на татарской раскладке, TalkBack-анонс смены языка, tt-поиск
> эмодзи (200 синонимов), ускоренное превью клавиш; **изменился ассет
> `emoji/emoji_search_v1.txt`** (tt-синонимы) — он осознанно не запинен,
> пинами охраняются только словари и биграммы (проверено: 16 пинов совпали);
> расширен baseline-профиль (теперь покрывает движок подсказок — на
> Play-канале применяется, при sideload игнорируется). Публикация, push и
> теги — действия оператора.
>
> **Перецелено 2026-08-30 на кандидата v1.9.5 / versionCode 21** (фаза 5 кампании
> реструктуризации, `docs/RESTRUCTURE.md`). Текущий артефакт —
> `dist/tatar-keyboard-1.9.5.apk` (2 111 775 Б), аудит — `docs/APK-AUDIT-1.9.5.md`.
> Всё, что написано ниже про v1.8.4 / versionCode 15 (шаги, порядок действий,
> принятые риски), остаётся историей и образцом процедуры — **галочки и числа тех
> прогонов на 1.9.5 не переносятся**. Различия кандидата 1.9.5, которые процедура
> обязана учесть: store-заметка — `metadata/en-US/changelogs/21.txt`; **из списка
> выбора убраны раскладки языков, кроме tt/ru/en** (пользовательское-видимое
> изменение, должно попасть в описание релиза); добавлен baseline-профиль — при
> установке из Google Play он применяется, при sideload молча игнорируется
> (фаза 4б). Публикация, push и теги — действия оператора.
>
> **Дополнение 2026-08-31 (DEV-2, воспроизводимая сборка).** Начиная с этого
> дерева release-сборка байт-в-байт воспроизводима: два (и три, включая прогон
> после полной очистки `build/` и `.gradle/`) `clean assembleRelease` дают
> одинаковый SHA-256 подписанного APK (`bf6225e5…09eae7b`, 2 111 775 Б, тот же
> размер, что у кандидата 1.9.5). Источником недетерминизма был Dependency Info
> Block (подписывался AGP эфемерным ключом на каждой сборке) — отключён
> `dependenciesInfo { includeInApk = false }` в `app/build.gradle`. Гейт в CI:
> джоба `reproducible` в `.github/workflows/ci.yml` (два чистых прогона +
> `cmp`). Это снимает блокер для F-Droid: при публикации там сверка идёт по
> копированию подписи поверх их пересборки, а наше unsigned-ядро теперь
> детерминировано.

Пошаговый release gate. Галочка означает
только проверенный результат именно текущего кандидата. Исторические результаты v1.1.0,
v1.2.0, v1.3.0, v1.4.0, v1.5.0, v1.6.0, v1.6.1, v1.7.0, v1.8.0, v1.8.1, v1.8.2, v1.8.3
и v1.8.4 не переносятся на новый APK и явно помечены как справочные.

## Кандидат — v1.8.4 / versionCode 15 (оператор, 2026-08-24)

**Читать до начала выпуска.** Версия про одно: любой жест получает ответ. Первое —
нарисованная подсказка всегда тапабельна: полоса перестала оставлять на экране кандидатов,
которых контроллер уже отвязал, а нажать их было нельзя (`1837d964`). Второе — четыре места,
где человек делал жест, ничего не происходило и ему не говорили почему: долгое нажатие по
подсказке при выключенном личном словаре и по словарному слову, клавиша эмодзи и пилюля
поиска эмодзи при непригодном наборе, ссылки «Политика конфиденциальности» и «Лицензия» без
браузера (`a3ef5540`). Третье — пропавшая по-русски строка настроек «Исправление слов»
(`b85d0661`). Отчёт миссии — `docs/archive/ux/FINAL-POLISH.md`. Бамп версии и changelog — `2f9e5715`.

Что меняется для человека: подсказка, которую видно, всегда та, которую можно нажать; каждый
из пяти перечисленных жестов теперь отвечает сообщением вместо тишины; на русском телефоне
строка «Исправление слов» больше не стоит английской между двумя русскими. Словари, таблицы
предсказания и наборы эмодзи **не менялись ни на байт**.

Следствия для этого файла:

- Кандидат этого чеклиста — **v1.8.4 / versionCode 15**, замороженный коммит `2f9e5715`.
  Все `[x]`, проставленные ниже по прогону 2026-08-24 (v1.8.4), относятся к нему.
- Ветка та же, что у 1.6.0…1.8.3 — `version-1.6.0`; новая не отводилась.
- Записи `[1.8.3]` и старше в `CHANGELOG.md`, а также `metadata/en-US/changelogs/14.txt`
  и старше не менялись. Версия описана разделом `[1.8.4]` и
  `metadata/en-US/changelogs/15.txt`. Разделы `[1.5.0]`…`[1.8.4]` вместе составляют то,
  что увидит пользователь, обновляющийся с 1.4.0.
- Числа всех прошлых прогонов, включая v1.8.3, остаются в файле как история и помечены;
  за свидетельство о v1.8.4 они не выдаются.
- Полный отчёт прогона под новой версией — `docs/archive/apk-audits/APK-AUDIT-1.8.4.md`.
- **Строк интерфейса прибавилось шесть** в трёх локалях (ru, tt, en); `<plurals>` среди них
  нет. Две из шести — русский перевод существующей пары ключей, которого не было вовсе.
  Отметка про строки ниже это учитывает.
- **Главный риск этой версии — правка полосы подсказок**, потому что она меняет то, что
  человек видит на каждом нажатии. Измерена и на JVM, и на устройстве
  (`docs/archive/apk-audits/APK-AUDIT-1.8.4.md`, раздел «Главный риск этого релиза»; сырые числа —
  `docs/archive/ux/final-polish/band-race-measured.txt`). Единственное, что осталось человеку с
  телефоном: посмотреть, не мигает ли полоса при обычном наборе.
- Обновление поверх предыдущей версии проверено (`docs/archive/apk-audits/APK-AUDIT-1.8.4.md`, раздел
  «Обновление поверх 1.8.3»): личный словарь цел, `firstInstallTime` не изменился.
- **Опубликованы ли v1.5.0…v1.8.3 — этот файл не знает.** Ни одна из них наружу не
  выходила по данным предыдущих аудитов; проверяется `git ls-remote --tags origin`.
  Решает оператор.

## Принятый риск: данные OpenSubtitles без лицензионного гранта (оператор, 2026-08-24)

> **Решено оператором 2026-08-31: вопрос закрыт («забудь»).** Риск принят
> окончательно, пункт снят с контроля релизов; дальнейшие подтверждения не
> требуются. Текст раздела ниже оставлен как историческая запись о том, что
> именно было принято.

Часть частотных данных для словарей получена из корпуса OpenSubtitles (OPUS, версия
v2024). **Лицензионного гранта у этого источника нет вообще** — это не «неясная» лицензия
и не спорная трактовка: на странице корпуса нет ни названной лицензии, ни разрешения
распространять или изменять, а раздающий прямо пишет, что текстом не владеет и снимет
файлы по обоснованному требованию. Дословные тексты — `docs/archive/dictionary/CORPUS.md` раздел 1.1.

Оператор 2026-08-24 решил использовать источник, зная это (вариант B вопроса миссии
`tt-corpus`; файл вопроса локальный, не публикуется). Решение осознанное и относится только к OpenSubtitles: QED и
TED2020 остаются непригодными, а правило «данные с невыясненной лицензией внутрь
приложения не попадают» для остальных источников продолжает действовать. Единственное,
о чём просит сам корпус, — ссылка на opensubtitles.org; она стоит в экране «Источники
данных» в приложении, в `app/src/main/assets/dictionaries/NOTICE.txt` и в отчёте
`docs/archive/dictionary/CORPUS-OS.md`.

**При публикации в каталоге это остаётся известным риском:** указать лицензию этой части
данных будет нечем. Решает оператор; этот пункт существует, чтобы решение не потерялось
между версиями.

- [x] Перед выпуском версии, которая **впервые везёт эти данные в ассетах**, оператор
  подтверждает, что риск ему по-прежнему приемлем. **Подтверждено оператором
  2026-08-31** («забудь») — вопрос закрыт окончательно, галочка проставлена по его
  прямому решению. История пункта: **этот выпуск был 1.9.0, и данные в нём
  поехали:** миссия `tt-dict-accept` приняла слова машинным правилом (`docs/archive/dictionary/DICT-ACCEPT.md`)
  и упаковала их в оба `.tdict.zlib`. Что именно поехало:
  27 134 русские формы и 226 татарских, из них со статусом `risk-accepted:no-grant`
  (слово держится на одних субтитрах) — числа в `docs/archive/dictionary/DICT-ACCEPT.md`, раздел про лицензии.

  **Обновлено 1.9.1: объём риска вырос, и подтверждение теперь стоит дороже.** Оператор снял
  планку «второй независимый источник» (`docs/archive/dictionary/DICT-WIDEN.md`), и в ассетах оказалось
  32 833 русские формы и 303 татарских. Слов со статусом `risk-accepted:no-grant`:
  **9 137 русских вместо 2 382** и **72 татарских вместо нуля**. Второе изменение
  качественное: до 1.9.1 татарский словарь не вёз ни одного слова, которого нет в Tatoeba, —
  это было следствием правила, требовавшего двух корпусов. Планка снята, свойство исчезло.
  Пункт по-прежнему невыполнен и по-прежнему ждёт оператора.
  *(Закрыто оператором 2026-08-31 — см. пометку в шапке раздела.)*
- [x] В том же выпуске поправить формулировки, которые честно говорили «ещё не в приложении».
  **Сделано в 1.9.0.** Секция `Prepared, not in the app yet` с экрана «Источники данных»
  убрана вместе со строкой `data_sources_prepared` во всех трёх локалях: все три источника
  стоят под `data_sources_in_app`, подписи трёх строк переписаны. Раздел
  `Conversational word frequencies -- measured and queued, NOT yet packed` в
  `app/src/main/assets/dictionaries/NOTICE.txt` переписан в `PACKED since 1.9.0` и называет
  числа. `DataSourcesScreenSourceContractTest` не удалён, а развёрнут: он теперь требует
  обратного — чтобы все три источника стояли как упакованные и чтобы заголовка
  «подготовлено» не осталось ни на экране, ни в ресурсах.
- [ ] Если оператор передумает, откат возможен, но он **дороже, чем вычеркнуть строки с
  риском**, и это надо знать заранее. Строк со статусом `risk-accepted:no-grant` в русской
  очереди 10 481, но у остальных 24 963 в колонке источников стоит `OpenSubtitles+Tatoeba` —
  слов, пришедших из одной Tatoeba, в очереди **нет ни одного**. Метка `clean` означает, что
  словоформа покрыта грантом CC BY 2.0 FR, а не что её частота получена без субтитров:
  у 22 908 из этих 24 963 чистая частота меньше 5 % от общей. Что именно уцелеет, показывает
  колонка `train_freq_clean` — она для этого и заведена. Полный откат — это перегенерация
  очередей по одной Tatoeba (`make_review.py` без файлов OpenSubtitles в аргументах), и цена
  отката числом — в `docs/archive/dictionary/CORPUS-OS.md`, раздел 10.

## Историческая запись: кандидат v1.8.3 / versionCode 14 (оператор, 2026-08-23)

Кандидат до 1.8.4. Версия про две вещи: приложение стало меньше (обе таблицы предсказания
перепакованы с K = 6 на K = 4) и повреждённый личный словарь перестал быть тупиком. Полный
отчёт — `docs/archive/apk-audits/APK-AUDIT-1.8.3.md`, frozen commit `bcc4391`, артефакт
`dist/tatar-keyboard-1.8.3.apk`, SHA-256 `a5b12793…`.

## Историческая запись: кандидат v1.8.2 / versionCode 13 (оператор, 2026-08-22)

Кандидат до 1.8.3. Версия патчевая и про одну вещь: клавиатура больше не молчит, когда у
неё что-то не получилось. Закрыты все восемь находок реестра `docs/archive/missions/SILENT-AUDIT.md` — места,
где неудача выглядела точно как успех: шесть в миссии `tt-personal-dict`
(`docs/archive/missions/PERSONAL-DICT-FIX.md`), две последние — решения оператора `B3-b` и `B2-b`. Бамп
версии и changelog — коммит `5022455`. Полный отчёт — `docs/archive/apk-audits/APK-AUDIT-1.8.2.md`. Наружу не
публиковалась.

## Историческая запись: кандидат v1.8.1 / versionCode 12 (оператор, 2026-08-22)

Кандидат до 1.8.2. Версия патчевая: ровно одна правка рабочего кода —
`RichInputConnection.deleteTextBeforeCursor` схлопывает пару границ выделения, как это делают
все остальные мутаторы класса (коммит `d725c1e`); бамп версии и changelog — `46f3b3a`. Правка
чинила два дефекта, которые оператор наблюдал на 1.8.0 на своём телефоне: тап по подсказке
иногда не вставлял слово, и подсказки не возвращались после стирания лишней буквы. Оба
воспроизведены на подписанной 1.8.0 и на 1.8.1 отсутствуют — `docs/archive/missions/TAP-REPRO.md`,
`docs/archive/apk-audits/APK-AUDIT-1.8.1.md`. Наружу не публиковалась.

## Историческая запись: кандидат v1.8.0 / versionCode 11 (оператор, 2026-08-22)

Кандидат до 1.8.1. Версия минорная: на русской раскладке появилось предсказание
следующего слова, которого в приложении не было. Русская таблица биграмм и выбор таблицы
по раскладке — миссия `tt-ru-bigrams` (`docs/archive/bigrams/RUSSIAN-BIGRAMS.md`, коммиты `62c76c5`,
`81c82ea`, `e5386d3`, `3aa6d21`); две мелочи поиска эмодзи и уборка мёртвого планшетного
кода — миссия `tt-small-fixes` (`docs/archive/emoji/SMALL-FIXES.md`, коммиты `ffc6002`, `0e7369c`,
`0356549`, `b5f94b5`); бамп версии и changelog — коммит `07b2c43`. Полный отчёт —
`docs/archive/apk-audits/APK-AUDIT-1.8.0.md`. Наружу не публиковалась.

## Историческая запись: кандидат v1.7.0 / versionCode 10 (оператор, 2026-08-21)

**Пометка: раздел описывает прошлого кандидата, не текущего.**

**Читать до начала выпуска.** Версия минорная, а не патч: в приложении появился второй
язык подсказок. Русский словарь на 100 000 форм и выбор словаря по активной раскладке
сделаны миссией `tt-russian-dict` (`docs/archive/dictionary/RUSSIAN-DICTIONARY.md`, коммит `a9a1acd`); бамп
версии и changelog — коммит `b2e2f33`.

Следствия для этого файла:

- Кандидат этого чеклиста — **v1.7.0 / versionCode 10**, коммит `b2e2f33`. Все `[x]`,
  проставленные ниже по прогону 2026-08-21 (v1.7.0), относятся к нему.
- Ветка та же, что у 1.6.0 и 1.6.1 — `version-1.6.0`; новая не отводилась.
- Записи `[1.6.1]` и старше в `CHANGELOG.md`, а также `metadata/en-US/changelogs/9.txt`
  и старше не менялись. Версия описана разделом `[1.7.0]` и
  `metadata/en-US/changelogs/10.txt`. Разделы `[1.5.0]`…`[1.7.0]` вместе составляют то,
  что увидит пользователь, обновляющийся с 1.4.0.
- Числа всех прошлых прогонов, включая v1.6.1, остаются в файле как история и помечены;
  за свидетельство о v1.7.0 они не выдаются.
- Полный отчёт прогона под новой версией — `docs/archive/apk-audits/APK-AUDIT-1.7.0.md`.
- **Опубликованы ли v1.5.0, v1.6.0 и v1.6.1 — этот файл не знает.** Ни одна из них наружу
  не выходила по данным предыдущих аудитов; проверяется `git ls-remote --tags origin`.
  Решает оператор.

## Историческая запись: кандидат v1.6.1 / versionCode 9 (оператор, 2026-08-20)

**Пометка: раздел описывает прошлого кандидата, не текущего.**

**Читать до начала выпуска.** Артефакт v1.6.0 был отправлен оператору на телефон
2026-08-20 и забракован им из-за двух дефектов поиска эмодзи: каретка стояла на 5dp правее
конца набранного запроса, а при пустом запросе под полем поиска висела пустая полоса
54dp с надписью «Наберите запрос». Оба починены миссией `tt-search-fixes`
(`docs/archive/emoji/EMOJI-SEARCH-FIXES.md`, коммиты `0f15289`, `43e1891`). Решением оператора от
2026-08-20 набор выходит **патч-версией 1.6.1 / versionCode 9**; 1.6.0 не выпускается.

Следствия для этого файла:

- Кандидат этого чеклиста — **v1.6.1 / versionCode 9**, коммит `652e1da`. Все `[x]`,
  проставленные ниже по прогону 2026-08-20 (v1.6.1), относятся к нему.
- Ветка та же, что у 1.6.0 — `version-1.6.0`; новая под патч не отводилась.
- Записи `[1.6.0]` и старше в `CHANGELOG.md`, а также `metadata/en-US/changelogs/8.txt`
  и старше не менялись. Патч описан разделом `[1.6.1]` и
  `metadata/en-US/changelogs/9.txt`. Разделы `[1.5.0]`, `[1.6.0]` и `[1.6.1]` вместе
  составляют то, что увидит пользователь, обновляющийся с 1.4.0.
- Числа всех прошлых прогонов, включая v1.6.0, остаются в файле как история и помечены;
  за свидетельство о v1.6.1 они не выдаются.
- Полный отчёт прогона под новой версией — `docs/archive/apk-audits/APK-AUDIT-1.6.1.md`.
- **Публикованы ли v1.5.0 и v1.6.0 — этот файл не знает.** Если ни один Release не
  выпущен, `versionCode` 7 и 8 просто не появляются в публичном канале, а пользователю
  достаётся сразу 1.6.1. Проверяется `git ls-remote --tags origin`. Решает оператор.

## Историческая запись: кандидат v1.6.0 / versionCode 8 (оператор, 2026-08-20)

**Пометка: раздел описывает прошлого кандидата, не текущего.**

**Читать до начала выпуска.** Поверх дерева, из которого нарезана v1.5.0, легли две
работы: доработка панели эмодзи — свайп между разделами и кожные тона
(`docs/archive/emoji/EMOJI-SKIN-TONES.md`, коммиты `02d594b`, `96fc862`) — и правки порога касания
(`docs/archive/ux/TOUCH-SLOP-TUNING.md`, коммиты `0dc5e9f`, `5bf6b37`, `9244e67`). Артефакт v1.5.0
собран **без них**, поэтому решением оператора от 2026-08-20 этот набор выходит
**отдельной версией 1.6.0 / versionCode 8**.

Следствия для этого файла:

- Кандидат этого чеклиста — **v1.6.0 / versionCode 8**, коммит `16f477b`. Все `[x]`,
  проставленные ниже по прогону 2026-08-20 (v1.6.0), относятся к нему.
- Ветка `version-1.6.0` отведена от `emoji-telegram` и вобрала
  `version-1.5.0` слиянием `a617f57`: релизные артефакты 1.5.0 (версия, changelog,
  `changelogs/7.txt`, `docs/archive/apk-audits/APK-AUDIT-1.5.0.md`, этот чеклист) сохранены целиком.
- Записи `[1.5.0]` и старше в `CHANGELOG.md`, а также `metadata/en-US/changelogs/7.txt`
  и старше не менялись. Новый набор описан разделом `[1.6.0]` и
  `metadata/en-US/changelogs/8.txt`.
- Числа прогонов 2026-07-25, 2026-08-18, 2026-08-19 и обоих прогонов 2026-08-20
  (v1.4.0 и v1.5.0) остаются в файле как история и помечены; за свидетельство о v1.6.0
  они не выдаются.
- Полный отчёт прогона под новой версией — `docs/archive/apk-audits/APK-AUDIT-1.6.0.md`.
- **Публикован ли v1.5.0 — этот файл не знает.** Если Release v1.5.0 не выпущен, версия
  1.5.0 остаётся локально проаудированной и не выпущенной, а пользователю достаётся
  сразу 1.6.0; `versionCode 7` при этом просто не публикуется. Решает оператор.

## Историческая запись: кандидат v1.5.0 / versionCode 7 (оператор, 2026-08-20)

**Пометка: раздел описывает прошлого кандидата, не текущего.** Поверх проаудированного дерева v1.4.0 легла одна работа —
вторая переделка панели эмодзи, теперь по образцу клиента Telegram, вместе с поиском по
эмодзи (`docs/archive/emoji/EMOJI-PANEL-TELEGRAM.md`, коммиты `bffcb73`, `736c838`, `f30589b`). Она
сделала артефакт v1.4.0 недействительным как кандидата на публикацию, поэтому решением
оператора от 2026-08-20 выходит **отдельной версией 1.5.0 / versionCode 7**.

Следствия для этого файла:

- Кандидат этого чеклиста — **v1.5.0 / versionCode 7**, коммит `4283944`. Все `[x]`,
  проставленные ниже по прогону 2026-08-20 (v1.5.0), относятся к нему.
- Записи `[1.4.0]` и старше в `CHANGELOG.md`, а также `metadata/en-US/changelogs/6.txt`
  и старше не менялись. Новый набор описан разделом `[1.5.0]` и
  `metadata/en-US/changelogs/7.txt`.
- Числа прогонов 2026-07-25, 2026-08-18, 2026-08-19 и 2026-08-20/v1.4.0 остаются в файле
  как история и помечены; за свидетельство о v1.5.0 они не выдаются.
- Полный отчёт прогона под новой версией — `docs/archive/apk-audits/APK-AUDIT-1.5.0.md`.

## Историческая запись: кандидат перенумерован 1.2.0/4 → 1.3.0/5 (оператор, 2026-08-18)

**Пометка 2026-08-20: раздел описывает прошлое перенумерование, не текущего кандидата.**
Актуальный кандидат — v1.6.0 / versionCode 8, см. раздел выше. Фазы E1–E5 и D3 (панель эмодзи, личный словарь, устойчивость
подсказок к опечаткам, предсказание следующего слова, автозамена с отменой) легли поверх
дерева, которое `CHANGELOG.md` описывал как 1.2.0, при неизменных `versionCode 4` /
`versionName "1.2.0"`. Аудит 2026-08-18 зафиксировал это как расхождение и оставил решение
оператору; решение принято: расширенный набор выходит **отдельной версией 1.3.0 /
versionCode 5**, changelog под 1.2.0 задним числом не переписывается.

Следствия для этого файла:

- Кандидат этого чеклиста — **v1.3.0 / versionCode 5**, коммит `1dc14afa`. Все `[x]`,
  проставленные ниже по прогону 2026-08-19, относятся к нему.
- Запись `[1.2.0]` в `CHANGELOG.md` и `metadata/en-US/changelogs/4.txt` не менялись: они
  по-прежнему описывают только D1. Новый набор описан разделом `[1.3.0]` и
  `metadata/en-US/changelogs/5.txt`.
- Числа прогонов 2026-07-25 и 2026-08-18 остаются в файле как история и помечены; за
  свидетельство о v1.3.0 они не выдаются.
- Полный отчёт прогона под новой версией — `docs/archive/apk-audits/APK-AUDIT-1.3.0.md`.

## Пробел в свидетельствах: аудированного артефакта больше нет (2026-07-26)

**Читать до начала выпуска.** Локальной копии `dist/tatar-keyboard-1.2.0.apk` не
существует: во время работы над фазой E1 она была перезаписана неопубликованной сборкой, а
затем удалена, чтобы неаудированный APK не ушёл в публикацию под именем релиза. Каталог
`dist/` пуст.

Все поля evidence ниже — размер **1 446 111 байт**, SHA-256
`26afd03f200f2939e5ce3b5f102bf4dcd93b5fbb8635161cd393b941cff13bcf`, дата 2026-07-25,
результат `cmp` с build output — относятся к этому артефакту, то есть к файлу, которого
локально больше нет. Проверить их прямо сейчас нельзя, и любая команда из блоков ниже,
читающая `dist/tatar-keyboard-1.2.0.apk`, не найдёт файла.

Пересборка их не восстанавливает. Установлено экспериментально: сборка из того же коммита
`4443a78` даёт ровно тот же размер 1 446 111 байт, но другой SHA-256 —
`7f7f33f90b189c2d99c8dd087668304760e92ca0f221946e663762da98273558`. Подпись APK
побайтово невоспроизводима, поэтому совпадение размера ничего не доказывает, а прежний
SHA-256 новой сборкой не получить в принципе. Сами аудированные байты сохранились только
на тестовом устройстве, где приложение установлено; устройство сейчас не подключено.

**Что из этого следует для выпуска.** Артефакт собирается заново, и artifact audit шагов 2
и 4 выполняется целиком по-новой на свежей сборке: размер, SHA-256, версия, permissions,
схемы подписи, сертификат и `cmp` с build output заполняются заново вместе с датой.
Отметки `[x]`, опирающиеся на утраченные числа, к новому артефакту не переносятся; старые
числа остаются в файле как история и не выдаются за свидетельство о новом APK.

**Что при этом НЕ произошло.** Наружу ничего не ушло: репозиторий ещё не открыт,
`versionCode 4` нигде не выложен, tag и GitHub Release не создавались, в IzzyOnDroid
заявка не подавалась. Отзывать нечего — требуется только повторный аудит перед выпуском.

## Device-UAT снят как критерий релиза (владелец, 2026-08-18)

Раздел 3 «Device-UAT и runtime budgets» ниже больше не блокирует релиз (решение принято при
номере кандидата 1.2.0 и действует для v1.6.0 без изменений) — решение
владельца. Сам раздел не переписан: ни один пункт не отмечен пройденным, потому что на
реальном устройстве по-прежнему ничего не измерено. Меняется только его вес в этом
чеклисте — из обязательного гейта он становится необязательным списком для будущих
итераций.

## Artifact audit v1.8.4 выполнен (2026-08-24)

Шаги 2 и 4 пройдены целиком на кандидате **v1.8.4 / versionCode 15**, сборка из
замороженного коммита `2f9e5715`; полный отчёт — `docs/archive/apk-audits/APK-AUDIT-1.8.4.md`. Заполнено новыми
числами: JVM suite (**952** теста, 0 падений, 0 ошибок, **0 skipped** — впервые за цепочку;
единственный `@Ignore`, оставленный миссией 1.8.1 на тесте второго дефекта полосы, снят, а
сам дефект починен), шесть питоновских наборов (**157** тестов, 1 skip), `lintVitalRelease`
(return-value `0`, отчёта об ошибках не создано), сборка debug и подписанного release APK из
одного checkout, no-INTERNET на исходнике и на обоих APK плюс backup-whitelist, версия
(`versionCode 15` / `1.8.4` в `output-metadata.json`, в манифесте артефакта и в
`dumpsys package` на устройстве), permission dump (только `VIBRATE`), размер (**2 496 781** Б
против потолка 3 145 728 Б, **+5 224 Б** к 1.8.3), `apksigner verify` → `Verifies` (v2 only,
один signer, RSA 4096, отпечаток `98ca6feb…42ad` совпал с требуемым), финальный SHA-256
`9a7d5736…`, копия `dist/tatar-keyboard-1.8.4.apk` с `cmp` без различий.

**Размер разложен по слагаемым:** `assets/` **±0 Б** (словари, таблицы предсказания и наборы
эмодзи в этой версии не трогались вовсе), `res/` ±0 Б, `resources.arsc` +4 324 Б (шесть новых
строк в трёх локалях), `classes.dex` +904 Б, `META-INF` −2 Б, прочее +1 Б. Запас до
обязательного потолка — **648 947 Б (20,63 %)** против 20,80 % на 1.8.3.

**Обновление поверх 1.8.3 проверено отдельно.** `adb install -r` на установленную подписанную
1.8.3: `firstInstallTime` не изменился (это и есть доказательство, что обновление, а не
переустановка), `lastUpdateTime` сдвинулся, `versionCode=15`. Личный словарь цел: слово,
выученное на 1.8.3, после обновления показывается в полосе той же ячейкой и видно на экране
«Сохранённые слова». Ассеты не менялись, поэтому переразвёртывания таблиц эта версия не
требует вообще.

**Главный риск версии — правка полосы подсказок — измерен на устройстве, а не только тестом.**
Обе сборки подписанные, на одном эмуляторе подряд, одна гарнесса, обе прогреты, n = 15 каждая:
1.8.3 дала 11 верных полос и **4 устаревшие**, и во всех четырёх тап по ячейке не сделал
ничего; 1.8.4 дала 12 верных и **3 пустые**, и все три вернулись со следующей же буквой. Доля
верной полосы не изменилась. Сырые числа — `docs/archive/ux/final-polish/band-race-measured.txt`,
снимок мёртвых кнопок на 1.8.3 — `docs/archive/ux/final-polish/dead-buttons-on-1.8.3.png`.

**Холодный старт:** медиана **266,5 мс**, худший 281,9, лучший 245,6 при инварианте 400,
n = 15 валидных из 20 попыток. На 1.8.3 было 271,2 / 287,2 при n = 12 из 19.

**Чего этот прогон не покрывает:** PSS не мерялся вовсе (эмулятор для него не годится), и
татарский интерфейс глазами не проверялся — система AVD не принимает `tt-RU` как локаль.
Оба пункта в списке «закрывает только человек» в `docs/archive/ux/FINAL-POLISH.md`.

## Историческая запись: artifact audit v1.8.3 (2026-08-23)

Шаги 2 и 4 пройдены целиком на кандидате **v1.8.3 / versionCode 14**, сборка из
замороженного коммита `bcc4391`; полный отчёт — `docs/archive/apk-audits/APK-AUDIT-1.8.3.md`. Заполнено новыми
числами: JVM suite (**913** тестов, 0 падений, 0 ошибок, 1 skipped — чужой `@Ignore` на
тесте второго дефекта полосы, который оператор решил оставить как есть), шесть питоновских
наборов (**157** тестов, 1 skip), `lintVitalRelease` (`No issues found.`, return-value `0`),
сборка debug и подписанного release APK из одного checkout, no-INTERNET на исходнике и на
обоих APK плюс backup-whitelist, версия (`versionCode 14` / `1.8.3` в `output-metadata.json`,
в манифесте артефакта и в `dumpsys package` на устройстве), permission dump (только
`VIBRATE`), размер (**2 491 557** Б против потолка 3 145 728 Б, **−91 188 Б** к 1.8.2),
`apksigner verify` → `Verifies` (v2 only, один signer, RSA 4096), финальный SHA-256
`a5b12793…`, копия `dist/tatar-keyboard-1.8.3.apk` с `cmp` без различий.

**Размер разложен по слагаемым, а не назван одним числом:** ассеты биграмм −100 203 Б
(отсечка K = 4), `resources.arsc` +6 592 Б (десять новых ключей и два `<plurals>` в трёх
локалях), `classes.dex` +2 425 Б (карантин после R8), остальное +2 Б; четыре байта разницы
между суммой записей и разницей файлов — заголовки zip. Запас до обязательного потолка —
**654 171 Б (20,80 %)**; он сокращался три релиза подряд (24,14 → 18,02 → 18,02 → 17,90 %)
и на этой версии впервые вырос.

**Обновление поверх 1.8.2 проверено отдельно — это главный риск версии.** Перепаковка меняет
SHA-256 ассетов, а SHA-256 сырого файла входит в имя файла на устройстве, поэтому обновившееся
устройство обязано распаковать обе таблицы заново. Стенд ставился именно так: пакет удалён,
поставлена `dist/tatar-keyboard-1.8.2.apk`, обе таблицы развёрнуты в K = 6, сохранено слово в
личный словарь, затем `adb install -r` на 1.8.3. Обе таблицы переразвернулись меньше чем за
секунду при первом наборе; **полоса подсказок вышла побайтно той же** на пяти татарских и
четырёх русских головах (попиксельное сравнение вырезок, девять из девяти без различий),
включая пустую полосу после повелительной формы `позвони`; личный словарь пережил обновление
байт-в-байт.

**Личный словарь и карантин проверены на подписанном release-артефакте целиком**: слово
сохраняется набором (порог — три употребления) и находится в списке, удаляется по кнопке с
подтверждением, «Стереть все слова» очищает каталог полностью. Весь путь карантина пройден
руками: обрезанный файл → извещение поверх **чужого** приложения → после рестарта процесса
извещения нет (сказали один раз) → карточка «Words that could not be read» с числом
уцелевших слов → «Bring the words back» возвращает слово и **не** удаляет копию → «Delete the
copy» с подтверждением → «Стереть всё» уносит остальное.

**Холодный старт замерен на этом артефакте** (эмулятор, не устройство): 259,6–287,2 мс,
медиана **271,2** мс при инварианте 400 мс — запас 112,8 мс (28,2 %) по худшему прогону,
n = 12 из девятнадцати попыток, семь несостоявшихся отброшены и не досчитаны. Медиана ниже
1.8.2 на 18,2 мс; направление совпадает с ожидаемым (распакованная татарская таблица меньше
на 131 810 Б, её открытие лежит на пути показа клавиатуры), но на выборках 12 и 9 отчёт этого
не утверждает как доказанное.

**Про `screencap` — поправка к трём прошлым прогонам.** Он на этом AVD **работает**: на
Messages, на редакторе контакта и на клавиатуре отдаёт нормальные PNG, и все свидетельства
этой версии сняты им. Нулевые файлы он отдаёт на **экране настроек приложения** — там, где
щупали личный словарь прошлые миссии, отсюда и прежний вывод. Там по-прежнему
`uiautomator dump`.

**Keystore не пересоздавался.** Подписано тем же ключом от 2026-08-18: сертификат SHA-256
`98ca6feb…`, public key SHA-256 `353e5d51…` — совпали с прогонами v1.4.0…v1.8.2 до
символа; требование досье «сертификат должен быть ровно 98ca6feb…» выполнено, останавливаться
и спрашивать оператора не пришлось. Сверка с историческим релизным `cdd8c535…` по-прежнему не
проходит: **upgrade path с v1.1.0 порван** решением оператора. Ключ существует в одном
экземпляре вне git — до публикации нужен его бэкап.

**Полный цикл сборки в этом прогоне был один.** Правило то же: в Release вкладывать именно
файл из `dist/`, чей SHA-256 указан выше, и не пересобирать перед публикацией.

## Историческая запись: artifact audit v1.8.2 (2026-08-22)

Числа кандидата до 1.8.3. Полный отчёт — `docs/archive/apk-audits/APK-AUDIT-1.8.2.md`, frozen commit
`5022455`. Кратко: JVM suite 859 тестов (87 файлов), питоновские наборы 155, размер
2 582 745 Б (запас 562 983, 17,90 %), SHA-256 `6de28e5d…`, сертификат `98ca6feb…`, холодный
старт 265,6–312,7 мс, медиана 289,4 (n = 9). Личный словарь проверен на подписанной сборке:
отказ удаления не убивал процесс, нечитаемый файл откладывался в один карантинный слот,
извещение показывалось один раз, «Стереть всё» убирало и копию. Наружу не публиковалась.

## Историческая запись: artifact audit v1.8.1 (2026-08-22)

Шаги 2 и 4 пройдены целиком на кандидате **v1.8.1 / versionCode 12**, сборка из
замороженного коммита `46f3b3a`; полный отчёт — `docs/archive/apk-audits/APK-AUDIT-1.8.1.md`. Числа того
прогона: JVM suite 815 тестов (0 падений, 1 skipped), шесть питоновских наборов,
`lintVitalRelease` чист, no-INTERNET на исходнике и на обоих APK, размер 2 578 765 Б,
финальный SHA-256 `93ca7854…`, сертификат `98ca6feb…`. Холодный старт 254,9–308,3 мс,
медиана 273,5 мс (n = 8). Оба дефекта, которые оператор видел на 1.8.0, проверены как A/B
на подписанных сборках; скриншоты — `docs/archive/apk-audits/apk-audit-1.8.1/`. **Сборка не байт-в-байт
воспроизводима:** в том прогоне две сборки одного дерева, различающиеся только текстом
`CHANGELOG.md`, дали APK одного размера с разными SHA-256.

## Историческая запись: artifact audit v1.8.0 (2026-08-22)


Шаги 2 и 4 пройдены целиком на кандидате **v1.8.0 / versionCode 11**, сборка из
замороженного коммита `07b2c43`; полный отчёт — `docs/archive/apk-audits/APK-AUDIT-1.8.0.md`. Заполнено
новыми числами: JVM suite (809 тестов, 0/0/0, 80 suite-файлов), шесть питоновских наборов
(155 тестов, 1 skip), `lintVitalRelease`, сборка debug и подписанного release APK из одного
checkout, no-INTERNET на исходнике и на обоих APK плюс backup-whitelist, версия
(`versionCode 11` / `1.8.0` в `output-metadata.json` и в манифесте артефакта), permission
dump (только `VIBRATE`), размер (2 578 753 Б против потолка 3 145 728 Б), `apksigner verify`
→ `Verifies` (v2 only, один signer, RSA 4096), финальный SHA-256 `e7d99887…`, копия
`dist/tatar-keyboard-1.8.0.apk` с `cmp` без различий.

**Холодный старт замерен на этом артефакте** (эмулятор, не устройство): 259,3–288,5 мс,
медиана 278,9 мс при инварианте 400 мс — запас 111,5 мс по худшему прогону. A/B против
переустановленной 1.7.0 на том же эмуляторе в том же сеансе — 265,2–323,1 мс, медиана
282,5 мс. Третий крупный ассет (русская таблица биграмм) старт не удлинил.

**Keystore не пересоздавался.** Подписано тем же ключом от 2026-08-18: сертификат SHA-256
`98ca6feb…`, SHA-1 `dc3e4a4d…`, public key SHA-256 `353e5d51…` — все три совпали с
прогонами 2026-08-18, 2026-08-19, v1.4.0…v1.7.0 до символа. Сверка с историческим
релизным `cdd8c535…` по-прежнему не проходит и проходить не будет: **upgrade path
с v1.1.0 порван** решением оператора. Ключ существует в одном экземпляре вне git — до
публикации нужен его бэкап.

**Сборка не байт-в-байт воспроизводима:** пересборка того же коммита даёт APK того же
размера с другим SHA-256. В Release вкладывать именно файл из `dist/`, чей SHA-256 указан
выше.

## Историческая запись: artifact audit v1.7.0 (2026-08-21)

**Пометка: относится к прошлому кандидату v1.7.0 / versionCode 10, а не к текущему.**

Шаги 2 и 4 пройдены целиком на кандидате **v1.7.0 / versionCode 10**, сборка из
замороженного коммита `b2e2f33`; полный отчёт — `docs/archive/apk-audits/APK-AUDIT-1.7.0.md`. Заполнено
новыми числами: JVM suite (798 тестов, 0/0/0, 80 suite-файлов), `lintVitalRelease`, сборка
debug и подписанного release APK из одного checkout, no-INTERNET на исходнике и на обоих
APK плюс backup-whitelist, версия (`versionCode 10` / `1.7.0` в `output-metadata.json` и в
манифесте артефакта), permission dump (только `VIBRATE`), размер (2 386 361 Б против
потолка 3 145 728 Б), `apksigner verify` → `Verifies` (v2 only, один signer, RSA 4096),
финальный SHA-256 `9e670053…`, копия `dist/tatar-keyboard-1.7.0.apk` с `cmp` без различий.

**Холодный старт замерен на этом артефакте** (эмулятор, не устройство): 289,5–316,8 мс,
медиана 299,2 мс при инварианте 400 мс; A/B против переустановленной 1.6.1 на том же
эмуляторе — 265,0–327,1 мс, медиана 294,6 мс. Второй словарь старт не удлинил.

**Keystore не пересоздавался.** Подписано тем же ключом от 2026-08-18: сертификат SHA-256
`98ca6feb…`, SHA-1 `dc3e4a4d…`, public key SHA-256 `353e5d51…` — все три совпали с
прогонами 2026-08-18, 2026-08-19, v1.4.0, v1.5.0, v1.6.0 и v1.6.1 до символа. Сверка с
историческим релизным `cdd8c535…` по-прежнему не проходит и проходить не будет: **upgrade
path с v1.1.0 порван** решением оператора. Ключ существует в одном экземпляре вне git — до
публикации нужен его бэкап.

**Сборка не байт-в-байт воспроизводима:** пересборка того же коммита даёт APK того же
размера с другим SHA-256. В Release вкладывать именно файл из `dist/`, чей SHA-256 указан
выше.

## Историческая запись: artifact audit v1.6.1 (2026-08-20)

**Пометка: относится к прошлому кандидату v1.6.1 / versionCode 9, а не к текущему.**

Шаги 2 и 4 пройдены целиком на кандидате **v1.6.1 / versionCode 9**, сборка из
замороженного коммита `652e1da`; полный отчёт — `docs/archive/apk-audits/APK-AUDIT-1.6.1.md`. Заполнено
новыми числами: JVM suite (789 тестов, 0/0/0, 79 suite-файлов), `lintVitalRelease`, сборка
debug и подписанного release APK из одного checkout, no-INTERNET на исходнике и на обоих
APK плюс backup-whitelist, версия (`versionCode 9` / `1.6.1` в `output-metadata.json` и в
манифесте артефакта), permission dump (только `VIBRATE`), размер (1 801 038 Б против
потолка 3 145 728 Б), `apksigner verify` → `Verifies` (v2 only, один signer, RSA 4096),
финальный SHA-256 `c48246ab…`, копия `dist/tatar-keyboard-1.6.1.apk` с `cmp` без различий.

**Keystore не пересоздавался.** Подписано тем же ключом от 2026-08-18: сертификат SHA-256
`98ca6feb…`, SHA-1 `dc3e4a4d…`, public key SHA-256 `353e5d51…` — все три совпали с
прогонами 2026-08-18, 2026-08-19, v1.4.0, v1.5.0 и v1.6.0 до символа. Сверка с историческим
релизным `cdd8c535…` по-прежнему не проходит и проходить не будет: **upgrade path с v1.1.0
порван** решением оператора. Ключ существует в одном экземпляре вне git — до публикации
нужен его бэкап.

**Сборка не байт-в-байт воспроизводима:** пересборка того же коммита даёт APK того же
размера с другим SHA-256. В Release вкладывать именно файл из `dist/`, чей SHA-256 указан
выше.

## Историческая запись: artifact audit v1.6.0 (2026-08-20)

**Пометка: относится к прошлому кандидату v1.6.0 / versionCode 8, а не к текущему.**

Шаги 2 и 4 пройдены целиком на кандидате **v1.6.0 / versionCode 8**, сборка из
замороженного коммита `16f477b`; полный отчёт — `docs/archive/apk-audits/APK-AUDIT-1.6.0.md`. Заполнено
новыми числами: JVM suite (780 тестов, 0/0/0, 78 suite-файлов), `lintVitalRelease`, сборка
debug и подписанного release APK из одного checkout, no-INTERNET на исходнике и на обоих
APK плюс backup-whitelist, версия (`versionCode 8` / `1.6.0` в `output-metadata.json` и в
манифесте артефакта), permission dump (только `VIBRATE`), размер (1 801 490 Б против
потолка 3 145 728 Б), `apksigner verify` → `Verifies` (v2 only, один signer, RSA 4096),
финальный SHA-256 `df8da11e…`, копия `dist/tatar-keyboard-1.6.0.apk` с `cmp` без различий.

**Keystore не пересоздавался.** Подписано тем же ключом от 2026-08-18: сертификат SHA-256
`98ca6feb…`, SHA-1 `dc3e4a4d…`, public key SHA-256 `353e5d51…` — все три совпали с
прогонами 2026-08-18, 2026-08-19, v1.4.0 и v1.5.0 до символа. Сверка с историческим
релизным `cdd8c535…` по-прежнему не проходит и проходить не будет: **upgrade path с v1.1.0
порван** решением оператора. Ключ существует в одном экземпляре вне git — до публикации
нужен его бэкап.

## Историческая запись: artifact audit v1.5.0 (2026-08-20)

**Пометка: относится к прошлому кандидату v1.5.0 / versionCode 7, а не к текущему.**

Шаги 2 и 4 пройдены целиком на кандидате **v1.5.0 / versionCode 7**, сборка из
замороженного коммита `4283944`; полный отчёт — `docs/archive/apk-audits/APK-AUDIT-1.5.0.md`. Заполнено
новыми числами: JVM suite (748 тестов, 0/0/0, 75 suite-файлов), `lintVitalRelease`, сборка
debug и подписанного release APK из одного checkout, no-INTERNET на исходнике и на обоих
APK плюс backup-whitelist, версия (`versionCode 7` / `1.5.0` в `output-metadata.json` и в
манифесте артефакта), permission dump (только `VIBRATE`), размер (1 798 198 Б против
потолка 3 145 728 Б), `apksigner verify` → `Verifies` (v2 only, один signer, RSA 4096),
финальный SHA-256 `29902121…`, копия `dist/tatar-keyboard-1.5.0.apk` с `cmp` без различий.

**Keystore не пересоздавался.** Подписано тем же ключом от 2026-08-18: сертификат SHA-256
`98ca6feb…`, SHA-1 `dc3e4a4d…`, public key SHA-256 `353e5d51…` — все три совпали с
прогонами 2026-08-18, 2026-08-19 и v1.4.0 до символа. Сверка с историческим релизным
`cdd8c535…` по-прежнему не проходит и проходить не будет: **upgrade path с v1.1.0 порван**
решением оператора. Ключ существует в одном экземпляре вне git — до публикации нужен его
бэкап.

## Историческая запись: artifact audit v1.4.0 (2026-08-20)

**Пометка: относится к прошлому кандидату v1.4.0 / versionCode 6, а не к текущему.**

Шаги 2 и 4 пройдены целиком на кандидате **v1.4.0 / versionCode 6**, сборка из
замороженного коммита `660c34f`; полный отчёт — `docs/archive/apk-audits/APK-AUDIT-1.4.0.md`. Заполнено
новыми числами: JVM suite (723 теста, 0/0/0, 74 suite-файла), `lintVitalRelease`, сборка
debug и подписанного release APK из одного checkout, no-INTERNET на исходнике и на обоих
APK плюс backup-whitelist, версия (`versionCode 6` / `1.4.0` в `output-metadata.json` и в
манифесте артефакта), permission dump (только `VIBRATE`), размер (1 733 028 Б против
потолка 3 145 728 Б), `apksigner verify` → `Verifies` (v2 only, один signer, RSA 4096),
финальный SHA-256 `fea08bdd…`, копия `dist/tatar-keyboard-1.4.0.apk` с `cmp` без различий.

**Keystore не пересоздавался.** Подписано тем же ключом от 2026-08-18: сертификат SHA-256
`98ca6feb…`, SHA-1 `dc3e4a4d…`, public key SHA-256 `353e5d51…` — все три совпали с
прогонами 2026-08-18 и 2026-08-19 до символа. Сверка с историческим релизным `cdd8c535…`
по-прежнему не проходит и проходить не будет: **upgrade path с v1.1.0 порван** решением
оператора. Ключ существует в одном экземпляре вне git — до публикации нужен его бэкап.

## Историческая запись: artifact audit v1.3.0 (2026-08-19)

**Пометка 2026-08-20: раздел описывает предыдущий прогон, не кандидата этого чеклиста.**
Артефакт v1.3.0 не содержал трёх клавиатурных правок; числа сохранены как история.

Шаги 2 и 4 пройдены целиком на кандидате **v1.3.0 / versionCode 5**, сборка из замороженного
коммита `1dc14afa`; полный отчёт — `docs/archive/apk-audits/APK-AUDIT-1.3.0.md`. Заполнено новыми числами: JVM
suite (709 тестов, 0/0/0, 73 suite-файла), `lintVitalRelease`, сборка debug и подписанного
release APK из одного checkout, no-INTERNET на исходнике и на обоих APK плюс
backup-whitelist, версия (`versionCode 5` / `1.3.0` в `output-metadata.json` и в манифесте
артефакта), permission dump (только `VIBRATE`), размер (1 731 072 Б против потолка
3 145 728 Б), `apksigner verify` → `Verifies` (v2 only, один signer, RSA 4096), финальный
SHA-256 `9f366d6c…`, копия `dist/tatar-keyboard-1.3.0.apk` с `cmp` без различий.

**Keystore не пересоздавался.** Подписано тем же ключом от 2026-08-18: сертификат SHA-256
`98ca6feb…`, SHA-1 `dc3e4a4d…`, public key SHA-256 `353e5d51…` — все три совпали с прошлым
прогоном до символа. Сверка с историческим релизным `cdd8c535…` по-прежнему не проходит и
проходить не будет: **upgrade path с v1.1.0 порван** решением оператора. Ключ существует в
одном экземпляре вне git — до публикации нужен его бэкап.

## Историческая запись: artifact audit 2026-08-18 (под номером 1.2.0/4)

**Описывает прошлый прогон, не кандидата этого чеклиста.** Тот же код, но артефакт
объявлял `versionCode 4` / `1.2.0`; перенумерован решением оператора — см. раздел
«Историческая запись: кандидат перенумерован» выше. Оставлено как история.

Шаги 2 и 4 пройдены заново и целиком на свежей сборке из коммита `9a2a3196`; полный отчёт —
`docs/archive/apk-audits/APK-AUDIT-2026-08-18.md`. Заполнено новыми числами: JVM suite (709 тестов, 0/0/0),
`lintVitalRelease`, сборка debug и подписанного release APK из одного checkout, no-INTERNET
на исходнике и на обоих APK, версия, permission dump, размер (1 731 072 Б против потолка
3 145 728 Б), `apksigner verify` → `Verifies` (v2 only, один signer, RSA 4096), финальный
SHA-256, копия в `dist/` с `cmp` без различий.

**Существенная оговорка: подписано новым ключом.** Релизного keystore на машине сборки не
оказалось вообще, и решением оператора от 2026-08-18 ключ сгенерирован заново (RSA 4096,
`CN=Tatar Keyboard`, сертификат SHA-256
`98ca6febfed6c146d81c1fdcfe52c79acf7aa926a1033d98b844a59803ec42ad`). С историческим
релизным `cdd8c535…` он не совпадает, **upgrade path с v1.1.0 порван**: установка поверх
старой сборки на тестовом устройстве потребует удаления приложения вместе с локальными
данными. Ключ существует в одном экземпляре вне git — до публикации нужен его бэкап, иначе
приложение нельзя будет обновить никогда. Раздел «Смена подписной идентичности» в отчёте
перечисляет последствия целиком.

Отдельно было зафиксировано расхождение по объёму кандидата: дерево `9a2a3196` содержало
фазы E1–E5 поверх набора, который `CHANGELOG.md` описывал как 1.2.0, при неизменных
`versionCode`/`versionName`. **Расхождение закрыто 2026-08-19** переномером на 1.3.0/5 —
см. раздел «Историческая запись: кандидат перенумерован» выше и `docs/archive/apk-audits/APK-AUDIT-1.3.0.md`.

## Историческое состояние на 2026-07-25 (кандидат тогда — v1.2.0/4)

**Раздел описывает прошлое состояние и оставлен как история.** Числа относятся к утраченному
артефакту 1.2.0; актуальное состояние кандидата v1.8.4 — в разделе «Artifact audit v1.8.4
выполнен» выше.

- Release candidate объявляет `1.2.0` / `versionCode 4`; CHANGELOG и Fastlane changelog
  подготовлены и сверены между собой.
- D1a–D1e реализованы, шесть находок независимого аудита закрыты и повторно
  отревьюированы (три линзы, все APPROVED_WITH_NOTES); поверх добавлен автопробел после
  принятой подсказки (`bacf177` + `c3ed443`, HEAD ветки). JVM-набор — **186 тестов,
  0 failures / 0 errors / 0 skipped** (по раундам: 140 → 161 → 177 → 186);
  `lintVitalRelease` — BUILD SUCCESSFUL.
- Финальный подписанный APK v1.2.0 **пересобран после автопробела** и проаудирован:
  1 446 111 байт, SHA-256
  `26afd03f200f2939e5ce3b5f102bf4dcd93b5fbb8635161cd393b941cff13bcf`, versionCode 4 /
  versionName 1.2.0, только permission `VIBRATE`, подпись v2 с историческим сертификатом.
  Evidence-поля ниже заполнены по этому артефакту. Release commit ещё не создан, поэтому
  frozen commit SHA остаётся открытым. **Записано на 2026-07-25; с 2026-07-26 самого
  артефакта локально нет** — см. раздел «Пробел в свидетельствах» в начале файла.
- Device-UAT на реальном Samsung, подтверждённые runtime/performance бюджеты, проверка
  TalkBack на устройстве и вычитка новых татарских строк носителем языка **не выполнены**.
  Есть только эмуляторный прогон (см. раздел 3) — частичное свидетельство, которое эти
  пункты не закрывает; более того, бюджет PSS на эмуляторе провален. Этот прогон шёл на
  **предыдущей** сборке, до автопробела, поэтому автопробел не проверен на устройстве
  вообще.
- Владелец вручную попробовал подсказки на реальном Samsung (серийник `R5CY8222TDP`) —
  функционально они работали, и именно это привело к запросу автопробела. Матрица не
  прогонялась, метрики не снимались, автопробела тогда ещё не было: ни одного checkbox
  раздела 3 этот факт не закрывает. Замер PSS на том же устройстве был начат, но валидных
  чисел не дал (снимался с открытым экраном настроек в том же процессе и с обрезанным
  выводом).
- Анонимные GitHub web/API/raw проверки по-прежнему получают HTTP 404. SSH-аутентификация
  позволяет push, но `gh` отсутствует, а доступный API token недействителен. Поэтому
  Public-доступ, GitHub Actions, tag/Release и анонимное скачивание APK не подтверждены.
- Для v1.1.0 был собран подписанный APK с валидной v2-подписью и проверенной цепочкой
  обновления. Это не evidence для APK кандидата. **Пометка 2026-08-19:** эталоном
  сертификата он тоже больше не является — релизный ключ сменился 2026-08-18, действующий
  отпечаток `98ca6feb…`; см. «Историческая справка v1.1.0» в конце файла.

## 1. Freeze исходников и версии

- [ ] Все intended изменения v1.8.4 находятся в одной проверяемой ветке; рабочее дерево
  не содержит случайных файлов, ключей или локальных конфигов. Частично: правки разложены
  на смысловые коммиты (`1837d964` полоса, `a3ef5540` молчаливые отказы, `b85d0661`
  строки трёх языков), бамп версии и changelog — `2f9e5715`; всё в той же ветке
  `version-1.6.0`. Дифф `2ad54f9d..2f9e5715` по `app/` затрагивает файлов кода: три
  файла рабочего кода (`SuggestionsController.kt`, `LatinIME.java`,
  `EmojiPanelController.kt`, `SettingsHostActivity.kt`), три файла строк и четыре файла
  тестов, из них три новых. **Ассеты не тронуты вовсе** — ни словари, ни таблицы
  предсказания, ни наборы эмодзи, ни раскладки; это подтверждено поэлементным сравнением
  архивов (`assets/` ±0 Б). Рабочее дерево на момент сборки чисто
  (`git status --porcelain` пуст), ключи и `dist/` git-ignored. Открыто: локальный
  служебный каталог миссий не отслеживается; полный diff человеком не просмотрен.
  **Release commit на этой версии создан локально** (`2f9e5715`) — этого требовал порядок
  «заморозить коммит и собрать из него»; наружу он не уходил. Отчёт миссии, аудит,
  свидетельства в `docs/archive/ux/final-polish/` и ретаргет этого файла закоммичены после `2f9e5715`;
  ни один из этих файлов в APK не попадает, артефакт не пересобирался.
- [x] `app/build.gradle` подтверждает `versionName "1.8.4"` и `versionCode 15`. Проверено
  2026-08-24 в дереве `2f9e5715` и подтверждено на собранном артефакте (`aapt2 dump badging`,
  `output-metadata.json`) и на устройстве (`dumpsys package`).
- [x] В `CHANGELOG.md` есть раздел `[1.8.4]` (2026-08-24: любой жест получает ответ), а
  `metadata/en-US/changelogs/15.txt` соответствует ему — сверено построчно 2026-08-24: пять
  строк, по одной на смысловой пункт записи (видимая подсказка всегда тапабельна; долгое
  нажатие всегда отвечает; клавиша эмодзи и поиск говорят, когда открывать нечего; ссылки
  говорят, когда их нечем открыть; настройка автокорректа снова по-русски), 310 Б при
  потолке Fastlane 500 Б. Записи `[1.8.3]` и старше, `changelogs/14.txt` и старше не
  менялись.
- [x] Новые татарские строки вычитаны носителем; имя проверяющего, дата и исправления
  записаны. **На v1.8.4 прибавилось шесть ключей** в `values/`, `values-ru/` и
  `values-tt/`: четыре новых сообщения (личный словарь выключен; слово из встроенного
  словаря; эмодзи недоступны; ссылку нечем открыть), одна новая формулировка пустого
  личного словаря для состояния «сохранение уже включено» и русский перевод существующей
  пары `tatar_autocorrect` / `tatar_autocorrect_summary`, которого не было вовсе.
  `<plurals>` среди них нет. Все внесены в `docs/archive/dictionary/TATAR-REVIEW-QUEUE.tsv` по действующему
  правилу оператора от 2026-08-20 — новые строки интерфейса принимаются без его отдельного
  подтверждения, `reviewer=dExNight`. **Формулировки оператор может переписать.** Исключение
  правила сохранено: словарные наборы (`docs/archive/missions/DICTIONARY-E3-TYPO-REVIEW.tsv`) под него не
  подпадают, по ним `approved` ставит только оператор лично, и туда эта версия не писала
  ничего.
- [x] В `docs/archive/dictionary/TATAR-REVIEW-QUEUE.tsv` нет ни одной строки в статусе `pending`, и у каждой
  строки заполнены `reviewer` и `date`. Перепроверено на дереве v1.8.4 машинно: **69 строк
  данных** (на замороженном коммите v1.8.3 по тому же счёту их 64 — прибавились пять новых
  татарских строк этой версии), все 69 в статусе `approved`, ни одной с пустым `reviewer`
  или `date`, у всех ровно девять полей.
- [ ] Происхождение данных в ассетах описано, и описание не опережает факты. **Переписано
  для 1.9.0:** `app/src/main/assets/dictionaries/NOTICE.txt` называет все три источника
  упакованными и говорит, сколько форм пришло от каждой группы; `app/src/main/assets/bigrams/
  NOTICE.txt` по-прежнему называет Leipzig единственным источником своих таблиц — и это
  по-прежнему правда: ни один `.tatbigr.zlib` не изменён ни на байт. Оба `.tdict.zlib`
  пересобраны, их SHA-256 и байты записаны в `docs/archive/dictionary/DICTIONARY-D1A.md`,
  `docs/archive/dictionary/RUSSIAN-DICTIONARY.md` и `docs/archive/apk-audits/APK-AUDIT-1.9.0.md`. Отдельный пункт про принятый
  риск OpenSubtitles — в разделе выше. Заодно исправлено 2026-08-24: `bigrams/NOTICE.txt`
  писал «top 6 successors per head», тогда как обе таблицы перепакованы на K = 4 ещё в
  v1.8.3; проверено чтением самих ассетов, максимум преемников на голову равен 4.
- [ ] `PRIVACY.md` называет каждое хранилище пользовательских данных, которое существует в
  собираемой сборке. **Долг тянется с v1.8.2 и на v1.8.4 не изменился:** карантинная копия
  (`no_backup/personal/…​.tpers.quarantine`, один слот на язык) и файл-метка извещения
  (`no_backup/personal/quarantine-notice-<язык>-s1-f1.flag`, один байт) в `PRIVACY.md` не
  описаны. Оба под `no_backup/`, наружу их не уносит и backup не поднимает — но в
  `PRIVACY.md` этого нет. Новых хранилищ v1.8.4 не заводит. Дописать.
- [ ] `git diff --check` и просмотр полного diff прошли перед release commit. Частично:
  рабочее дерево на момент сборки чисто, полный diff человеком ещё не просматривался. На
  этой версии он небольшой и весь про интерфейс: `git diff bcc4391..2f9e5715`.

## 2. Автоматические локальные gates

Запускать из чистого checkout с JDK 17 и Android SDK. Локальный signing config должен
оставаться git-ignored; секреты не копируются в CI.

```sh
./gradlew clean test lintVitalRelease assembleDebug assembleRelease \
  --rerun-tasks --console=plain
bash scripts/check-no-internet.sh app/build/outputs/apk/debug/app-debug.apk
bash scripts/check-no-internet.sh app/build/outputs/apk/release/app-release.apk
apksigner verify --verbose --print-certs \
  app/build/outputs/apk/release/app-release.apk
```

- [x] Полный JVM unit suite прошёл без failures/errors; записано число тестов — **913**
  (0 failures / 0 errors / **1 skipped**), 90 suite-файлов. Сборка 2026-08-23 из коммита
  `bcc4391` (кандидат v1.8.3). Было 859 в 87 suite на v1.8.2: **+54 теста, +3 файла** —
  52 в трёх новых файлах миссии `tt-quarantine` (`PersonalQuarantineSalvageTest` 19,
  `PersonalQuarantineRecoveryTest` 22, `PersonalQuarantineScreenSourceContractTest` 11),
  +1 в `PersonalQuarantineNoticeSourceContractTest` (9 → 10) и +1 в `TatBigrValidatorTest`
  (19 → 20) от миссии биграмм. Разложение сходится точно. **Красное до правки** снималось
  миссиями-авторами, а не этой: `docs/archive/missions/QUARANTINE.md`, раздел 5 (отдельный worktree на
  `HEAD`; один тест там честно назван зелёным и за закрытую дыру не выдаётся) и
  `docs/archive/bigrams/BIGRAM-ADJACENCY.md`. Эта миссия отвечает за то, что весь набор зелёный на
  замороженном коммите. **Единственный skipped — чужой и намеренный:**
  `TapReproTest.tappingAWordTheStripIsPaintingAlwaysReachesTheEditor` помечен `@Ignore`
  миссией v1.8.1 под второй, независимый дефект полосы подсказок, который оператор решил
  оставить как есть; эта версия его не трогала.
  Дополнительно прогнаны шесть питоновских наборов (`tests/`): **157 тестов, 0 падений,
  1 skip** — 27 `bigram_asset_pack`, 18 `bigram_pack`, 6 `dictionary_coverage`,
  31 `dictionary_pack`, 35 `emoji_pack`, 40 `typo_pack`. Было 155; +2 — тесты, которыми
  миссия биграмм закрепила правило соседства и отбор голов.
- [x] `lintVitalRelease` завершился `BUILD SUCCESSFUL` (сборка 2026-08-23, коммит `bcc4391`);
  задачи выполнялись, а не брались из кэша — прогон шёл с `--rerun-tasks` и `clean` в той
  же команде, 84 задачи. Текстовый отчёт самой задачи — `No issues found.`, return-value
  файл — `0` (`app/build/intermediates/lint_vital_*/release/lintVitalReportRelease/`).
- [x] Debug и signed release APK собраны из того же frozen checkout: 2026-08-23 из коммита
  `bcc4391` одной командой собраны `app-debug.apk` (4 130 006 Б) и подписанный
  `app-release.apk` (2 491 557 Б) — `BUILD SUCCESSFUL`. Полный цикл сборки в этой миссии был
  **один**, поэтому истории 1.8.1 про два разных SHA-256 у одинакового по составу артефакта
  здесь нет.
- [x] Source, debug APK и release APK не содержат `android.permission.INTERNET`.
  Сборка 2026-08-23 (v1.8.3): `scripts/check-no-internet.sh` — exit 0 на исходнике и на
  обоих APK, оба уровня (source manifest, merged manifest артефакта) плюс backup-whitelist;
  в обоих APK единственное permission — `VIBRATE` (отдельный `aapt2 dump permissions`).
  Отдельно про эту версию: и карантинная копия личного словаря, и файл-метка извещения
  лежат в `no_backup/personal/`, то есть backup их не поднимает и наружу не уносит.
- [x] Release APK подписан APK Signature Scheme v2 (v1=false, v2=true,
  v3/v3.1/v3.2/v4=false), один signer, RSA 4096, `DN CN=Tatar Keyboard`; `apksigner verify`
  → `Verifies`, exit 0. Проверено 2026-08-23 на `app-release.apk`, копия
  `dist/tatar-keyboard-1.8.3.apk` побайтно та же (`cmp` без различий).
- [ ] Signer certificate SHA-256 совпадает с историческим релизным сертификатом v1.1.0,
  upgrade path сохранён. **Не выполняется и выполнено не будет:** решением оператора от
  2026-08-18 сгенерирован новый релизный ключ, сертификат артефакта v1.8.3 —
  `98ca6febfed6c146d81c1fdcfe52c79acf7aa926a1033d98b844a59803ec42ad` против исторического
  `cdd8c5350ddc86f13cd89b5bfb55ca33c13efba77beb4d4ccb75d5e6b961b09e`. Ключ этой миссией не
  пересоздавался — отпечатки совпали с прогонами 2026-08-18, 2026-08-19 и
  v1.4.0…v1.8.2. Установка поверх сборки со старым ключом невозможна без удаления
  приложения. Пункт оставлен незакрытым намеренно, чтобы факт разрыва upgrade path не
  потерялся; см. «Подписная идентичность» в `docs/archive/apk-audits/APK-AUDIT-1.8.0.md`.
  Отдельно: досье миссии v1.8.3 требовало, чтобы сертификат был **ровно** `98ca6feb…`, иначе
  остановиться и спросить оператора. Отпечаток совпал, останавливаться не пришлось.
- [x] `output-metadata.json` (`versionCode 14`, `versionName "1.8.3"`, `variantName
  release`) и manifest-разбор артефакта подтверждают версию. Сборка 2026-08-23:
  metadata release — `versionCode 14`, `versionName "1.8.3"`, `variantName release`,
  `applicationId org.tatarkeyboard.ime`, `minSdkVersionForDexing 24`; `aapt2 dump badging` —
  `versionCode='14'`, `versionName='1.8.3'`, `minSdkVersion:'24'`, `targetSdkVersion:'37'`,
  `compileSdkVersion='37'`. Установленный на эмуляторе артефакт по `dumpsys package` —
  `versionCode=14 minSdk=24 targetSdk=37`, `versionName=1.8.3`: щупалась именно эта сборка.
- [x] Единственное запрошенное runtime/platform permission — `VIBRATE`; permission dump
  артефакта просмотрен целиком и содержит ровно одну строку. Сборка 2026-08-23 (v1.8.3):
  `aapt2 dump permissions` на release и на debug даёт по две строки — `package:` и
  `uses-permission: name='android.permission.VIBRATE'`, больше ничего.
- [x] APK не превышает 3 145 728 байт: **2 491 557** байт (сборка 2026-08-23,
  подписанный артефакт v1.8.3). Запас до потолка — **654 171 байт (20,80 %)**. Против v1.8.2
  (2 582 745 Б) артефакт **уменьшился на 91 188 байт**, и это разложено по слагаемым:
  ассеты биграмм −100 203 (отсечка K = 4), `resources.arsc` +6 592 (десять новых ключей и
  два `<plurals>` в трёх локалях), `classes.dex` +2 425 (карантин после R8), прочее +2,
  четыре байта — заголовки zip. Запас сокращался три релиза подряд (24,14 → 18,02 → 18,02 →
  17,90 %) и здесь впервые вырос.
  **Целевой бюджет D1 ≤ 1,7 MiB перестал быть критерием:** оператор снял его
  2026-08-21 («размер на данном этапе вообще не важен»), оставив только жёсткий потолок
  3 МБ; `BRIEF.md` приведён в соответствие миссией `tt-cleanup` — там сказано, что
  действующая планка одна и новая не назначена. Назначать ли новую — решение оператора.
  Обязательный потолок чеклиста 3 145 728 Б — pass.
  Историческое число 1 446 111 Б относится к утраченному артефакту от 2026-07-25.
- [x] **Обновление поверх предыдущей версии не ломает распаковку ассетов.** Новый пункт
  этой версии: перепаковка меняет SHA-256 ассетов, а он входит в имя файла на устройстве,
  поэтому обновившееся устройство распаковывает обе таблицы заново. Проверено 2026-08-23 на
  эмуляторе: пакет удалён, поставлена `dist/tatar-keyboard-1.8.2.apk`, обе таблицы
  развёрнуты в K = 6 (644 148 и 635 838 Б), сохранено слово в личный словарь, затем
  `adb install -r dist/tatar-keyboard-1.8.3.apk`. Обе таблицы переразвернулись при первом
  наборе меньше чем за секунду (512 338 и 505 768 Б), полоса подсказок вышла **побайтно
  той же** на девяти проверенных головах двух языков, личный словарь пережил обновление
  байт-в-байт. **Найдено при этом и не починено:** старые таблицы с диска не уходят
  (`MAX_FINAL_ARTIFACTS = 2`), поэтому на устройстве после обновления лежат обе версии —
  плюс 1 018 106 Б. `docs/archive/apk-audits/APK-AUDIT-1.8.3.md`, «Что нашлось по дороге», п. 1.

### Evidence финального локального артефакта v1.8.3 — сборка 2026-08-23

Заполнять после последней пересборки; `PENDING` не заменять промежуточными числами.
Полный отчёт прогона — `docs/archive/apk-audits/APK-AUDIT-1.8.3.md`.

**Подписано ключом от 2026-08-18, keystore не пересоздавался и не трогался.** Схема подписи
та же, что у исторического артефакта, но сам сертификат другой — сверка с историческим
релизным не проходит и upgrade path с v1.1.0 порван. См. «Подписная идентичность» в
`docs/archive/apk-audits/APK-AUDIT-1.8.0.md`.

| Проверка | Результат v1.8.3 (2026-08-23) |
|---|---|
| Frozen commit SHA | `bcc439192d89e4e275ed56bdd7d784056c05cde4` (HEAD ветки `version-1.6.0`, новая ветка не отводилась); **release commit создан локально** — этого требовал порядок «заморозить коммит и собрать из него»; наружу он не уходил. Отчёт и ретаргет этого файла закоммичены после и в APK не попадают |
| Дата сборки | 2026-08-23, артефакт release 21:16:53 |
| Среда | Linux 6.18.44-1-lts x86-64, OpenJDK 17.0.20.1, Gradle 9.6.0, AGP 9.2.1, build-tools 37.0.0, Python 3.14.7 |
| JVM tests | 913 tests, 0 failures / 0 errors / 1 skipped (90 suite-файлов); +54 теста и +3 файла к v1.8.2; skipped — чужой `@Ignore` миссии v1.8.1 на тесте второго, независимого дефекта полосы |
| Красное до правки | снималось миссиями-авторами, не этой: `docs/archive/missions/QUARANTINE.md` раздел 5 и `docs/archive/bigrams/BIGRAM-ADJACENCY.md`. Здесь проверено только то, что весь набор зелёный на замороженном коммите |
| Python tests | 157 tests в шести наборах `tests/`, 0 падений, 1 skip |
| Состояние сборки | `./gradlew --offline clean test lintVitalRelease assembleDebug assembleRelease --rerun-tasks --console=plain` → `BUILD SUCCESSFUL`, 84 задачи выполнены |
| `lintVitalRelease` | `BUILD SUCCESSFUL`; отчёт задачи — `No issues found.`, return-value — `0` |
| Manifest version | versionName `1.8.3`, versionCode `14` (release и debug), minSdk 24, targetSdk 37, compileSdk 37 |
| Permissions / no-INTERNET | только `android.permission.VIBRATE` в release и в debug; `check-no-internet.sh` exit 0 на исходнике и обоих APK, оба уровня плюс backup-whitelist. Карантинная копия и метка извещения лежат в `no_backup/`, backup их не поднимает |
| APK signing schemes | v2 only (v1=false, v2=true, v3=false, v3.1=false, v3.2=false, v4=false), 1 signer, RSA 4096, `DN CN=Tatar Keyboard`; `apksigner verify` → `Verifies`, exit 0 |
| Signer certificate SHA-256 | `98ca6febfed6c146d81c1fdcfe52c79acf7aa926a1033d98b844a59803ec42ad` (public key SHA-256 `353e5d51f23bbf7c0d5587ab59320bc61ffa438b8ae92764b91914d3a3dc8466`) — тот же ключ, что 2026-08-18, 2026-08-19 и v1.4.0…v1.8.2; **совпал с отпечатком, которого требовало досье миссии**. С историческим `cdd8c5350ddc86f13cd89b5bfb55ca33c13efba77beb4d4ccb75d5e6b961b09e` НЕ совпадает, upgrade path с v1.1.0 порван |
| APK size, bytes | 2 491 557 (release, signed) — pass против обязательного 3 145 728 (запас 654 171, 20,80 %); **−91 188 Б** к v1.8.2 (ассеты −100 203, `resources.arsc` +6 592, `classes.dex` +2 425); целевая планка D1 1,7 MiB снята оператором 2026-08-21; debug 4 130 006 |
| Обновление поверх 1.8.2 | обе таблицы переразвёрнуты меньше чем за секунду при первом наборе; полоса подсказок **побайтно та же** на 9 из 9 проверенных голов (5 tt, 4 ru, включая пустую после повелительной `позвони`); личный словарь пережил обновление байт-в-байт. На устройстве при этом **+1 018 106 Б**: старые таблицы не удаляются (`MAX_FINAL_ARTIFACTS = 2`) — записано как находка, не починено |
| Холодный старт (эмулятор) | 259,6–287,2 мс, медиана 271,2 мс (n=12 из девятнадцати попыток, семь несостоявшихся отброшены и не досчитаны, `drop_caches` перед каждым прогоном) при инварианте 400 мс — запас 112,8 мс (28,2 %) по худшему прогону; v1.8.2 в том же стенде — 265,6–312,7 мс, медиана 289,4 (n=9). Направление совпадает с ожидаемым (татарская таблица на диске меньше на 131 810 Б), но на таких выборках это не доказательство. Не заменяет замера на реальном устройстве |
| Функциональная проверка | личный словарь на подписанном артефакте: сохранение набором (порог 3 употребления), удаление с подтверждением, «Стереть всё» (каталог пуст). Карантин целиком: обрезанный файл → извещение поверх чужого приложения → после рестарта процесса извещения нет → карточка «Words that could not be read» с числом уцелевших → «Bring the words back» возвращает слово и не удаляет копию → «Delete the copy» → «Стереть всё». Свидетельства — `screencap` (на клавиатуре и чужих приложениях он на этом AVD **работает**) и `uiautomator dump` (на экранах приложения, где `screencap` отдаёт нулевые файлы) |
| Final APK SHA-256 | `a5b1279357f0f8e0f03ba746ed88a750bcd3e369a23e1e5555ce6c2901fdb85c`; debug — `6a1c6a16dc57ca5c02ee59e1ca393e88f69e9bf96b3008528feaa329b07470a1`. Полный цикл сборки был **один**, но правило прежнее: **сборка не байт-в-байт воспроизводима**, в Release вкладывать файл из `dist/` |
| Шаг 4 (`dist/`) | `dist/tatar-keyboard-1.8.3.apk` создан, `cmp` с build output без различий, оба 2 491 557 Б, SHA-256 копии совпадает; mtime копии 21:17:35 — позже сборки, и после копирования не пересобиралось |

### Evidence артефакта v1.8.2 (история, сборка 2026-08-22)

Числа кандидата до 1.8.3. Полный отчёт — `docs/archive/apk-audits/APK-AUDIT-1.8.2.md`. Frozen commit
`5022455a50839b4e67bc365b2bae8a52c8670c41`; JVM 859 tests (0 failures / 0 errors / 1 skipped,
87 suite-файлов), Python 155 tests, release 2 582 745 Б, SHA-256
`6de28e5da06621ac814ec2eb2b6cfd7bbfe14a81540ad1cc1de8315e57e99d0f`, debug 4 213 794 Б
(`746fe607…`), холодный старт 265,6–312,7 мс при медиане 289,4 мс (n=9), тот же сертификат
`98ca6feb…`, копия `dist/tatar-keyboard-1.8.2.apk`. SHA-256 этой копии сверен заново
2026-08-23 и совпал. Наружу не публиковалась.

### Evidence артефакта v1.8.1 (история, сборка 2026-08-22)

Числа кандидата до 1.8.2. Полный отчёт — `docs/archive/apk-audits/APK-AUDIT-1.8.1.md`. Frozen commit не
создавался, дерево — HEAD `46f3b3a42fc6168bd22aeadb821acf00f79de727`; JVM 815 tests
(0 failures / 0 errors / 1 skipped, 82 suite-файла), Python 155 tests, release 2 578 765 Б,
SHA-256 `93ca78547fddd5a803199f8320610b2e3516dc6400eec6a3e11cdc59f0a3b156`, debug
4 204 126 Б (`84f92f8a…`), холодный старт 254,9–308,3 мс при медиане 273,5 мс (n=8), тот же
сертификат `98ca6feb…`, копия `dist/tatar-keyboard-1.8.1.apk`. В том прогоне две сборки
одного дерева, различавшиеся только текстом `CHANGELOG.md`, дали APK одного размера с
разными SHA-256 — отсюда правило «вкладывать файл из `dist/`». Наружу не публиковалась.

### Evidence артефакта v1.8.0 (история, сборка 2026-08-22)

Числа кандидата до 1.8.1. Полный отчёт — `docs/archive/apk-audits/APK-AUDIT-1.8.0.md`. Frozen commit
`07b2c4323b59874463b6bf69d67259218e60f1e6`, JVM 809 tests (0/0/0, 80 suite-файлов),
Python 155 tests, release 2 578 753 Б, SHA-256
`e7d99887b5316644ab8344e37916b312304850793324664f42755212371d1be5`, debug 4 204 130 Б
(`72f0c96b…`), холодный старт 259,3–288,5 мс при медиане 278,9 мс, тот же сертификат
`98ca6feb…`, копия `dist/tatar-keyboard-1.8.0.apk`. Наружу не публиковалась.

### Evidence артефакта v1.7.0 (история, сборка 2026-08-21)

Заполнять после последней пересборки; `PENDING` не заменять промежуточными числами.
Полный отчёт прогона — `docs/archive/apk-audits/APK-AUDIT-1.7.0.md`.

**Подписано ключом от 2026-08-18, keystore не пересоздавался.** Схема подписи та же, что у
исторического артефакта, но сам сертификат другой — сверка с историческим релизным не
проходит и upgrade path с v1.1.0 порван. См. «Подписная идентичность» в
`docs/archive/apk-audits/APK-AUDIT-1.7.0.md`.

| Проверка | Результат v1.7.0 (2026-08-21) |
|---|---|
| Frozen commit SHA | `b2e2f333b0f355646cccc39d2d135dcd03ab44c2` (HEAD ветки `version-1.6.0`, новая ветка не отводилась); release commit по-прежнему не создан |
| Дата сборки | 2026-08-21, артефакты 03:57:02 (release) и 03:56:56 (debug) |
| Среда | Linux 6.18.44-1-lts x86-64, OpenJDK 17.0.20.1, Gradle 9.6.0, AGP 9.2.1, build-tools 37.0.0 |
| JVM tests | 798 tests, 0 failures / 0 errors / 0 skipped (80 suite-файлов) |
| Состояние сборки | `./gradlew --offline clean test lintVitalRelease assembleDebug assembleRelease --rerun-tasks --console=plain` → `BUILD SUCCESSFUL`, 84 actionable tasks: 84 executed |
| `lintVitalRelease` | `BUILD SUCCESSFUL` |
| Manifest version | versionName `1.7.0`, versionCode `10` (release и debug), minSdk 24, targetSdk 37 |
| Permissions / no-INTERNET | только `android.permission.VIBRATE` в release и в debug; `check-no-internet.sh` exit 0 на обоих, оба уровня плюс backup-whitelist |
| APK signing schemes | v2 only (v1=false, v2=true, v3=false, v3.1=false, v3.2=false, v4=false), 1 signer, RSA 4096, `DN CN=Tatar Keyboard`; `apksigner verify` → `Verifies`, exit 0 |
| Signer certificate SHA-256 | `98ca6febfed6c146d81c1fdcfe52c79acf7aa926a1033d98b844a59803ec42ad` (SHA-1 `dc3e4a4de7b9d6edb74eaef19f14abe37049dd4f`, public key SHA-256 `353e5d51f23bbf7c0d5587ab59320bc61ffa438b8ae92764b91914d3a3dc8466`) — тот же ключ, что 2026-08-18, 2026-08-19, v1.4.0, v1.5.0, v1.6.0 и v1.6.1, все три отпечатка совпали. **С историческим `cdd8c5350ddc86f13cd89b5bfb55ca33c13efba77beb4d4ccb75d5e6b961b09e` НЕ совпадает, upgrade path с v1.1.0 порван** |
| APK size, bytes | 2 386 361 (release, signed) — pass против обязательного 3 145 728 (запас 759 367, 24,14 %); целевая планка D1 1,7 MiB снята оператором 2026-08-21; debug 4 011 846 |
| Холодный старт (эмулятор) | 289,5–316,8 мс, медиана 299,2 мс (n=6, `drop_caches` перед каждым прогоном) при инварианте 400 мс; A/B на том же эмуляторе с переустановленной v1.6.1 — 265,0–327,1 мс, медиана 294,6 мс. Не заменяет замера на реальном устройстве |
| Final APK SHA-256 | `9e670053176907e410cf9528e92bb496a672f08f8d597db91812d3df41af3a62`; debug — `7e3648ea517bf50901c27d6be261b76a16c8f010d75e5b51370f5aee7c08b754`. **Сборка не байт-в-байт воспроизводима** — пересборка того же коммита даёт APK того же размера с другим SHA-256; в Release вкладывать файл из `dist/` |
| Шаг 4 (`dist/`) | `dist/tatar-keyboard-1.7.0.apk` создан, `cmp` с build output без различий, оба 2 386 361 Б, SHA-256 копии совпадает, `apksigner verify` на копии → `Verifies` |

### Evidence артефакта v1.6.1 (история, сборка 2026-08-20)

**Пометка: относится к прошлому кандидату v1.6.1 / versionCode 9.**

Заполнять после последней пересборки; `PENDING` не заменять промежуточными числами.
Полный отчёт прогона — `docs/archive/apk-audits/APK-AUDIT-1.6.1.md`.

**Подписано ключом от 2026-08-18, keystore не пересоздавался.** Схема подписи та же, что у
исторического артефакта, но сам сертификат другой — сверка с историческим релизным не
проходит и upgrade path с v1.1.0 порван. См. «Подписная идентичность» в
`docs/archive/apk-audits/APK-AUDIT-1.6.1.md`.

| Проверка | Результат v1.6.1 (2026-08-20) |
|---|---|
| Frozen commit SHA | `652e1dad8aca06939165ffcd804b092eaf2894bc` (HEAD ветки `version-1.6.0`, новая ветка под патч не отводилась); release commit по-прежнему не создан |
| Дата сборки | 2026-08-20, артефакты 16:37:22 (release) и 16:37:18 (debug) |
| Среда | Linux 6.18.44-1-lts x86-64, OpenJDK 17.0.20.1, Gradle 9.6.0, AGP 9.2.1, build-tools 37.0.0 |
| JVM tests | 789 tests, 0 failures / 0 errors / 0 skipped (79 suite-файлов) |
| Состояние сборки | `./gradlew --offline clean test lintVitalRelease assembleDebug assembleRelease --rerun-tasks --console=plain` → `BUILD SUCCESSFUL`, 84 actionable tasks: 84 executed |
| `lintVitalRelease` | `BUILD SUCCESSFUL` |
| Manifest version | versionName `1.6.1`, versionCode `9` (release и debug), minSdk 24, targetSdk 37 |
| Permissions / no-INTERNET | только `android.permission.VIBRATE` в release и в debug; `check-no-internet.sh` exit 0 на обоих, оба уровня плюс backup-whitelist |
| APK signing schemes | v2 only (v1=false, v2=true, v3=false, v3.1=false, v3.2=false, v4=false), 1 signer, RSA 4096, `DN CN=Tatar Keyboard`; `apksigner verify` → `Verifies`, exit 0 |
| Signer certificate SHA-256 | `98ca6febfed6c146d81c1fdcfe52c79acf7aa926a1033d98b844a59803ec42ad` (SHA-1 `dc3e4a4de7b9d6edb74eaef19f14abe37049dd4f`, public key SHA-256 `353e5d51f23bbf7c0d5587ab59320bc61ffa438b8ae92764b91914d3a3dc8466`) — тот же ключ, что 2026-08-18, 2026-08-19, v1.4.0, v1.5.0 и v1.6.0, все три отпечатка совпали. **С историческим `cdd8c5350ddc86f13cd89b5bfb55ca33c13efba77beb4d4ccb75d5e6b961b09e` НЕ совпадает, upgrade path с v1.1.0 порван** |
| APK size, bytes | 1 801 038 (release, signed) — pass против обязательного 3 145 728 (запас 1 344 690, 42,75 %); целевой D1 1,7 MiB не выполнен (1,7176 MiB, перебор 18 459 Б); debug 3 424 475 |
| Final APK SHA-256 | `c48246ab4301848d71c02c116c3e235708e47a5f75013c67d8e914fb60d0fac5`; debug — `082c145b85e8746102280544c26b2a9e46c5fc07869c3b331e6a39dc2a7dc677`. **Сборка не байт-в-байт воспроизводима** — пересборка того же коммита даёт APK того же размера с другим SHA-256; в Release вкладывать файл из `dist/` |
| Шаг 4 (`dist/`) | `dist/tatar-keyboard-1.6.1.apk` создан, `cmp` с build output без различий, оба 1 801 038 Б, SHA-256 копии совпадает, `apksigner verify` на копии → `Verifies` |

### Evidence артефакта v1.6.0 (история, сборка 2026-08-20)

**Пометка: относится к прошлому кандидату v1.6.0 / versionCode 8.**

| Проверка | Результат v1.6.0 (2026-08-20) |
|---|---|
| Frozen commit SHA | `16f477b` (HEAD ветки `version-1.6.0`, отведена от `emoji-telegram`, вобрала `version-1.5.0` слиянием `a617f57`); release commit по-прежнему не создан |
| Дата сборки | 2026-08-20, артефакты 15:46:40 (release) и 15:46:32 (debug) |
| Среда | Linux 6.18.44-1-lts x86-64, OpenJDK 17.0.20.1, Gradle 9.6.0, AGP 9.2.1, build-tools 37.0.0 |
| JVM tests | 780 tests, 0 failures / 0 errors / 0 skipped (78 suite-файлов) |
| Состояние сборки | `./gradlew clean test lintVitalRelease assembleDebug assembleRelease --rerun-tasks --console=plain` → `BUILD SUCCESSFUL`, 84 actionable tasks: 84 executed |
| `lintVitalRelease` | `BUILD SUCCESSFUL` |
| Manifest version | versionName `1.6.0`, versionCode `8` (release и debug), minSdk 24, targetSdk 37 |
| Permissions / no-INTERNET | только `android.permission.VIBRATE` в release и в debug; `check-no-internet.sh` exit 0 на обоих, оба уровня плюс backup-whitelist; в исходном манифесте INTERNET 0 вхождений |
| APK signing schemes | v2 only (v1=false, v2=true, v3=false, v3.1=false, v3.2=false, v4=false), 1 signer, RSA 4096, `DN CN=Tatar Keyboard`; `apksigner verify` → `Verifies`, exit 0 |
| Signer certificate SHA-256 | `98ca6febfed6c146d81c1fdcfe52c79acf7aa926a1033d98b844a59803ec42ad` (SHA-1 `dc3e4a4de7b9d6edb74eaef19f14abe37049dd4f`, public key SHA-256 `353e5d51f23bbf7c0d5587ab59320bc61ffa438b8ae92764b91914d3a3dc8466`) — тот же ключ, что 2026-08-18, 2026-08-19, v1.4.0 и v1.5.0, все три отпечатка совпали. **С историческим `cdd8c5350ddc86f13cd89b5bfb55ca33c13efba77beb4d4ccb75d5e6b961b09e` НЕ совпадает, upgrade path с v1.1.0 порван** |
| APK size, bytes | 1 801 490 (release, signed) — pass против обязательного 3 145 728 (запас 1 344 238); целевой D1 1,7 MiB не выполнен (1,7180 MiB, перебор 18 911 Б); debug 3 424 707 |
| Final APK SHA-256 | `df8da11e9767732d8186423a28cf0249210735012298136eddfdd502280d180a`; debug — `b61688a3706b61d3f82372fe3c544547364b0f4d46eab9906a76a33b218eb52b` |
| Шаг 4 (`dist/`) | `dist/tatar-keyboard-1.6.0.apk` создан, `cmp` с build output без различий, оба 1 801 490 Б, SHA-256 копии совпадает, `apksigner verify` на копии → `Verifies` |

### Evidence артефакта v1.5.0 (история, сборка 2026-08-20)

**Пометка: относится к прошлому кандидату v1.5.0 / versionCode 7.**

**Подписано ключом от 2026-08-18, keystore не пересоздавался.** Схема подписи та же, что у
исторического артефакта, но сам сертификат другой — сверка с историческим релизным не
проходит и upgrade path с v1.1.0 порван. См. «Подписная идентичность» в
`docs/archive/apk-audits/APK-AUDIT-1.5.0.md`.

| Проверка | Результат v1.5.0 (2026-08-20) |
|---|---|
| Frozen commit SHA | `4283944e570c46344baffd3523edc5d55c0cddf0` (HEAD ветки `version-1.5.0`, отведена от `emoji-telegram`); release commit по-прежнему не создан |
| Дата сборки | 2026-08-20, артефакты 04:22:36 (release) и 04:22:36 (debug) |
| Среда | Linux 6.18.44-1-lts x86-64, OpenJDK 17.0.20.1, Gradle 9.6.0, AGP 9.2.1, build-tools 37.0.0 |
| JVM tests | 748 tests, 0 failures / 0 errors / 0 skipped (75 suite-файлов) |
| Состояние сборки | `./gradlew clean test lintVitalRelease assembleDebug assembleRelease --rerun-tasks --console=plain` → `BUILD SUCCESSFUL`, 84 actionable tasks: 84 executed |
| `lintVitalRelease` | `BUILD SUCCESSFUL` |
| Manifest version | versionName `1.5.0`, versionCode `7` (release и debug), minSdk 24, targetSdk 37 |
| Permissions / no-INTERNET | только `android.permission.VIBRATE` в release и в debug; `check-no-internet.sh` exit 0 на обоих, оба уровня плюс backup-whitelist; в исходном манифесте INTERNET 0 вхождений |
| APK signing schemes | v2 only (v1=false, v2=true, v3=false, v3.1=false, v3.2=false, v4=false), 1 signer, RSA 4096, `DN CN=Tatar Keyboard`; `apksigner verify` → `Verifies`, exit 0 |
| Signer certificate SHA-256 | `98ca6febfed6c146d81c1fdcfe52c79acf7aa926a1033d98b844a59803ec42ad` (SHA-1 `dc3e4a4de7b9d6edb74eaef19f14abe37049dd4f`, public key SHA-256 `353e5d51f23bbf7c0d5587ab59320bc61ffa438b8ae92764b91914d3a3dc8466`) — тот же ключ, что 2026-08-18, 2026-08-19 и v1.4.0, все три отпечатка совпали. **С историческим `cdd8c5350ddc86f13cd89b5bfb55ca33c13efba77beb4d4ccb75d5e6b961b09e` НЕ совпадает, upgrade path с v1.1.0 порван** |
| APK size, bytes | 1 798 198 (release, signed) — pass против обязательного 3 145 728 (запас 1 347 530); целевой D1 1,7 MiB не выполнен (1,7147 MiB, перебор 15 619 Б — ассет поиска); debug 3 416 427 |
| Final APK SHA-256 | `29902121835feb0a3bfa659f22557db8107732422d8e37766dfbd5ea56d1cb83`; debug — `de5dbb709da3eec826a65ecde1244f24f5b3f1cf698a1d61454cb1062064646d` |
| Шаг 4 (`dist/`) | `dist/tatar-keyboard-1.5.0.apk` создан, `cmp` с build output без различий, оба 1 798 198 Б, SHA-256 копии совпадает, `apksigner verify` на копии → `Verifies` |

### Evidence артефакта v1.4.0 (история, сборка 2026-08-20)

Числа ниже относятся к прошлому кандидату v1.4.0 / versionCode 6 и за свидетельство о
v1.5.0 не выдаются. Полный отчёт того прогона — `docs/archive/apk-audits/APK-AUDIT-1.4.0.md`.

| Проверка | Результат v1.4.0 (2026-08-20) |
|---|---|
| Frozen commit SHA | `660c34f386ed6b00e0327a60434bdd00083d9467` (HEAD ветки `version-1.4.0`, отведена от `emoji-panel-redesign`); release commit по-прежнему не создан |
| Дата сборки | 2026-08-20, артефакты 02:49:20 (release) и 02:49:20 (debug) |
| Среда | Linux 6.18.44-1-lts x86-64, OpenJDK 17.0.20.1, Gradle 9.6.0, AGP 9.2.1, build-tools 37.0.0 |
| JVM tests | 723 tests, 0 failures / 0 errors / 0 skipped (74 suite-файла) |
| Состояние сборки | `./gradlew clean test lintVitalRelease assembleDebug assembleRelease --rerun-tasks --console=plain` → `BUILD SUCCESSFUL`, 84 actionable tasks: 84 executed |
| `lintVitalRelease` | `BUILD SUCCESSFUL` |
| Manifest version | versionName `1.4.0`, versionCode `6` (release и debug), minSdk 24, targetSdk 37 |
| Permissions / no-INTERNET | только `android.permission.VIBRATE` в release и в debug; `check-no-internet.sh` exit 0 на обоих, оба уровня плюс backup-whitelist; в исходном манифесте INTERNET 0 вхождений |
| APK signing schemes | v2 only (v1=false, v2=true, v3=false, v3.1=false, v3.2=false, v4=false), 1 signer, RSA 4096, `DN CN=Tatar Keyboard`; `apksigner verify` → `Verifies`, exit 0 |
| Signer certificate SHA-256 | `98ca6febfed6c146d81c1fdcfe52c79acf7aa926a1033d98b844a59803ec42ad` (SHA-1 `dc3e4a4de7b9d6edb74eaef19f14abe37049dd4f`, public key SHA-256 `353e5d51f23bbf7c0d5587ab59320bc61ffa438b8ae92764b91914d3a3dc8466`) — тот же ключ, что 2026-08-18 и 2026-08-19, все три отпечатка совпали. **С историческим `cdd8c5350ddc86f13cd89b5bfb55ca33c13efba77beb4d4ccb75d5e6b961b09e` НЕ совпадает, upgrade path с v1.1.0 порван** |
| APK size, bytes | 1 733 028 (release, signed) — pass против 3 145 728 (запас 1 412 700) и против 1,7 MiB (1,6528 MiB); debug 3 328 760 |
| Final APK SHA-256 | `fea08bdd76e74c091e009ba73f18f6a3eac09a3574c9f256786717c5c218113c`; debug — `9e258d0c6998f1cc25a5ebcced8dce29020fc741989dfd102a79fe3581c3df6d` |
| Шаг 4 (`dist/`) | `dist/tatar-keyboard-1.4.0.apk` создан, `cmp` с build output без различий, оба 1 733 028 Б, SHA-256 копии совпадает, `apksigner verify` на копии → `Verifies` |

### Evidence артефакта v1.3.0 (история, сборка 2026-08-19)

**Пометка 2026-08-20: таблица описывает предыдущий прогон, не кандидата этого чеклиста.**
Артефакт v1.3.0 не содержал трёх клавиатурных правок; числа сохранены как история и за
свидетельство о v1.5.0 не выдаются. Полный отчёт того прогона — `docs/archive/apk-audits/APK-AUDIT-1.3.0.md`.

| Проверка | Результат v1.3.0 (2026-08-19) |
|---|---|
| Frozen commit SHA | `1dc14afa5576f9781ba56f13df4e94d0c67de61c` (HEAD ветки `version-1.3.0`, отведена от `apk-audit-2026-08-18`); release commit по-прежнему не создан |
| Дата сборки | 2026-08-19, артефакты 12:53:44 (debug) и 12:53:52 (release) |
| Среда | Linux, OpenJDK 17.0.20, Gradle 9.6.0, AGP 9.2.1, build-tools 37.0.0 |
| JVM tests | 709 tests, 0 failures / 0 errors / 0 skipped (73 suite-файла) |
| Состояние сборки | `./gradlew clean test lintVitalRelease assembleDebug assembleRelease --rerun-tasks --console=plain` → `BUILD SUCCESSFUL`, 84 actionable tasks: 84 executed |
| `lintVitalRelease` | `BUILD SUCCESSFUL` |
| Manifest version | versionName `1.3.0`, versionCode `5` (release и debug), minSdk 24, targetSdk 37 |
| Permissions / no-INTERNET | только `android.permission.VIBRATE` в release и в debug; `check-no-internet.sh` exit 0 на обоих, оба уровня плюс backup-whitelist; в исходном манифесте INTERNET 0 вхождений |
| APK signing schemes | v2 only (v1=false, v2=true, v3=false, v3.1=false, v3.2=false, v4=false), 1 signer, RSA 4096, `DN CN=Tatar Keyboard`; `apksigner verify` → `Verifies`, exit 0 |
| Signer certificate SHA-256 | `98ca6febfed6c146d81c1fdcfe52c79acf7aa926a1033d98b844a59803ec42ad` (SHA-1 `dc3e4a4de7b9d6edb74eaef19f14abe37049dd4f`, public key SHA-256 `353e5d51f23bbf7c0d5587ab59320bc61ffa438b8ae92764b91914d3a3dc8466`) — тот же ключ, что 2026-08-18, все три отпечатка совпали. **С историческим `cdd8c5350ddc86f13cd89b5bfb55ca33c13efba77beb4d4ccb75d5e6b961b09e` НЕ совпадает, upgrade path с v1.1.0 порван** |
| APK size, bytes | 1 731 072 (release, signed) — pass против 3 145 728 (запас 1 414 656) и против 1,7 MiB (1,6509 MiB); debug 3 326 176 |
| Final APK SHA-256 | `9f366d6cddfdb172fbd254a27afd91f238e51ac94bc34e5531040521cdaa74ea`; debug — `45d1707386c0f1ed1fcdee317f5d25771abeb3c3046a0481be14b05cc08b03ca` |
| Шаг 4 (`dist/`) | `dist/tatar-keyboard-1.3.0.apk` создан, `cmp` с build output без различий, оба 1 731 072 Б, SHA-256 копии совпадает, `apksigner verify` на копии → `Verifies` |

### Evidence артефакта под номером 1.2.0 (история, пересборка 2026-08-18)

**Пометка 2026-08-19, действует и на 2026-08-20: таблица описывает прошлый прогон, не
кандидата этого чеклиста.** Тот же код, но артефакт объявлял `versionCode 4` / `1.2.0`; перенумерован решением
оператора — см. раздел «Историческая запись: кандидат перенумерован» выше. Числа сохранены
как история и за свидетельство о v1.4.0 не выдаются.

Заполнять после последней пересборки; `PENDING` не заменять промежуточными числами.
Полный отчёт прогона — `docs/archive/apk-audits/APK-AUDIT-2026-08-18.md`.

**Подписано новым ключом.** Релизного keystore на машине сборки не оказалось, и решением
оператора от 2026-08-18 ключ сгенерирован заново. Схема подписи та же, что у исторического
артефакта, но сам сертификат другой — сверка с историческим релизным не проходит и upgrade
path с v1.1.0 порван. См. «Смена подписной идентичности» в `docs/archive/apk-audits/APK-AUDIT-2026-08-18.md`.

| Проверка | Результат v1.2.0 (2026-08-18) |
|---|---|
| Frozen commit SHA | `9a2a31960426d93482cb98bb951e46a399e5b3fc` (HEAD ветки `apk-audit-2026-08-18`, отведена от `e5-bigram-prediction`); release commit по-прежнему не создан |
| Дата сборки | 2026-08-18, artefacts 16:36:45 (debug) и 16:36:51 (release) |
| Среда | Linux, OpenJDK 17.0.20, Gradle 9.6.0, AGP 9.2.1, build-tools 37.0.0 |
| JVM tests | 709 tests, 0 failures / 0 errors / 0 skipped (73 suite-файла) |
| Состояние сборки | `./gradlew clean test lintVitalRelease assembleDebug assembleRelease --rerun-tasks --console=plain` → `BUILD SUCCESSFUL`, 84 actionable tasks: 84 executed |
| `lintVitalRelease` | `BUILD SUCCESSFUL` |
| Manifest version | versionName `1.2.0`, versionCode `4` (release и debug), minSdk 24, targetSdk 37 |
| Permissions / no-INTERNET | только `android.permission.VIBRATE` в release и в debug; `check-no-internet.sh` exit 0 на обоих, оба уровня; в исходном манифесте INTERNET 0 вхождений |
| APK signing schemes | v2 only (v1=false, v2=true, v3=false, v3.1=false, v3.2=false, v4=false), 1 signer, RSA 4096, `DN CN=Tatar Keyboard`; `apksigner verify` → `Verifies`, exit 0 |
| Signer certificate SHA-256 | `98ca6febfed6c146d81c1fdcfe52c79acf7aa926a1033d98b844a59803ec42ad` (SHA-1 `dc3e4a4de7b9d6edb74eaef19f14abe37049dd4f`, public key SHA-256 `353e5d51f23bbf7c0d5587ab59320bc61ffa438b8ae92764b91914d3a3dc8466`). **Новый ключ от 2026-08-18 — с историческим `cdd8c5350ddc86f13cd89b5bfb55ca33c13efba77beb4d4ccb75d5e6b961b09e` НЕ совпадает, upgrade path с v1.1.0 порван** |
| APK size, bytes | 1 731 072 (release, signed) — pass против 3 145 728 (запас 1 414 656) и против 1,7 MiB (1,6509 MiB); debug 3 326 176 |
| Final APK SHA-256 | `18fc03695ec6421c745e536abda32c7cdf2d8779acecaae095d5a5e561a77256`; debug — `9a30f152204edddb204fd93966b7b39f74a958710ea41c439e3babddedff96f4` |
| Шаг 4 (`dist/`) | `dist/tatar-keyboard-1.2.0.apk` создан, `cmp` с build output без различий, оба 1 731 072 Б, SHA-256 копии совпадает, `apksigner verify` на копии → `Verifies` |

### Evidence утраченного артефакта (история, 2026-07-25)

**Пометка 2026-07-26: таблица описывает утраченный артефакт.** Файла, к которому относятся
эти числа, локально больше нет, и проверить их сейчас нельзя — см. раздел «Пробел в
свидетельствах» в начале файла. Числа сохранены как история; перед выпуском таблица
заполняется заново по свежей сборке, включая дату и SHA-256, который у новой сборки будет
другим при том же размере.

| Проверка | Результат v1.2.0 (историческое, 2026-07-25) |
|---|---|
| Frozen commit SHA | `PENDING` — артефакт собран из рабочего дерева на `c3ed443` (код — `bacf177`, автопробел включён), release commit ещё не создан |
| JVM tests | 186 tests, 0 failures / 0 errors / 0 skipped |
| Состояние сборки | пересобрана после автопробела `bacf177`; mtime `app/build/outputs/apk/release/app-release.apk` и копии в `dist/` — 2026-07-25 02:02:38, позже коммита `c3ed443` (02:02:24) |
| `lintVitalRelease` | `BUILD SUCCESSFUL` |
| Manifest version | versionName `1.2.0`, versionCode `4` |
| Permissions / no-INTERNET | только `android.permission.VIBRATE`; INTERNET нет ни в исходном манифесте, ни в release APK (debug APK не собирался) |
| APK signing schemes | v2 only (v1=false, v2=true, v3=false, v3.1=false, v4=false), 1 signer, RSA 4096 |
| Signer certificate SHA-256 | `cdd8c5350ddc86f13cd89b5bfb55ca33c13efba77beb4d4ccb75d5e6b961b09e` (совпадает с историческим релизным; SHA-1 `b0f3fa16a46a5a7c7b7c663165fd2e1c2e56a889`, public key SHA-256 `a9524b37c7ebff271e9ddf670d1c19ceb44bf0981542277deee3cb83bbf6757c`) |
| APK size, bytes | 1 446 111 |
| Final APK SHA-256 | `26afd03f200f2939e5ce3b5f102bf4dcd93b5fbb8635161cd393b941cff13bcf` |

## 3. Device-UAT и runtime budgets

Host/JVM tests не являются device evidence. Записать устройство, Android/API, клавиатуру
производителя и сырые измерения там, где требуется метрика.

- [ ] Обычный ввод tt/ru/en не регрессировал; включение/выключение opt-in работает,
  значение по умолчанию OFF.
- [ ] Подсказки работают end-to-end для татарского subtype: 0–3 результата, все ячейки
  и крайние пиксели нажимаются, tap безопасно выполняет exact delete+commit.
- [ ] Автопробел проверен на устройстве: принятая подсказка вставляется вместе с пробелом;
  перед пунктуацией и там, где пробел уже стоит, лишний пробел не появляется; следующее
  нажатие пробела не превращает вставленный пробел в точку; автозаглавная обновляется
  сразу. Не проверялось нигде — ни на эмуляторе, ни на реальном устройстве.
- [ ] Полная privacy matrix скрывает полосу и запрещает lookup/commit в password,
  email, URI, filter, autocomplete, `NO_SUGGESTIONS`, non-text и selection/mid-word.
- [ ] TalkBack проверен вручную: обход только заполненных virtual nodes, полные labels,
  click action, обновление/скрытие и отсутствие stale action.
- [ ] Samsung/целевое устройство: rotation/recreation, navigation insets, moreKeys,
  hardware keyboard и `onComputeInsets` не теряют touchable regions.
- [ ] Total PSS показанной клавиатуры уложился в правило дельт на фазу и в абсолютный
  потолок, пересчитанный по первому валидному замеру на реальном устройстве
  (`docs/archive/PROPOSALS.md`, раздел «Бюджет памяти (PSS)»); приложены сырые значения — полный вывод
  `dumpsys meminfo` обоих плеч и медианы.
- [ ] Cold start < 400 ms; приложены отдельные прогоны и медиана.
- [ ] Prefix compute p95 ≤ 5 ms и request→guarded UI publish p95 ≤ 16 ms на целевом
  устройстве; visible stale results = 0.
- [ ] Hot draw/touch allocations = 0, janky frames ≤ 1%, FD/PSS не растут на повторных
  lifecycle cycles; записаны инструменты и результаты.

### Частичное свидетельство v1.8.3: эмулятор (не закрывает ни один пункт выше)

Прогон 2026-08-23 на AVD `tatar_e5_test` (Android 11 / API 30, x86-64, headless,
`-gpu swiftshader_indirect`, плотность 440, 1080×2280) на **подписанном release-артефакте**
`dist/tatar-keyboard-1.8.3.apk`, установленном **обновлением поверх 1.8.2** (`adb install -r`,
версия на устройстве сверена через `dumpsys package`: `versionCode=14`, `versionName=1.8.3`).
Это не Samsung и не One UI; ни один пункт списка выше не закрывается.

Стенд ставился с нуля намеренно: на эмуляторе лежало испорченное состояние (сборка миссии
`tt-quarantine` — версия 1.8.2, а таблицы уже K = 4), поэтому пакет был удалён целиком и
поставлена `dist/tatar-keyboard-1.8.2.apk`, тот самый файл, чей SHA-256 сверен выше.

Слова набирались **на собственной клавиатуре приложения**: `adb shell input text` кириллицу
не умеет, а клавиатура — один `Canvas`-View без целей для `uiautomator`, поэтому нажатия шли
`input tap` по калиброванной сетке (11 клавиш по 98,18 px, шесть по 180 px в пятом татарском
ряду; ряды татарской раскладки выше русской на высоту пятого ряда). Состояние читалось
`screencap` (на клавиатуре и чужих приложениях), `uiautomator dump` (на экранах приложения) и
прямым просмотром `/data/data/org.tatarkeyboard.ime/no_backup/personal/` и
`/data/user_de/0/org.tatarkeyboard.ime/files/` под `adb root` — ни одного вывода «на глаз».

- **Обновление поверх не ломает распаковку ассетов.** Обе таблицы развернулись заново при
  первом же наборе, меньше чем за секунду: tt `…fb686476…` 644 148 Б → `…1e614224…`
  512 338 Б, ru `…48f8ec6a…` 635 838 Б → `…264b09fc…` 505 768 Б. Пауз, залипаний и пустой
  полосы в момент распаковки не наблюдалось.
- **Предсказание после перепаковки не изменилось ни на пиксель.** Полоса снята скриншотом
  после девяти слов до обновления и после него; попиксельное сравнение вырезок полосы дало
  «различий нет» **девять раз из девяти**. Проверенные головы: tt `бер` (генә · үк ·
  тапкыр), `мин` (дә · үзем · бу), `бу` (хакта · юлы · турыда), `ул` (вакытта · шулай · да),
  `без` (бу · дә · аны); ru `по` (словам · его · данным), `при` (этом · помощи ·
  поддержке), `том` (числе · же · что) и `позвони` — **пусто в обеих версиях**, потому что
  повелительная форма не является головой таблицы (`docs/archive/bigrams/BIGRAM-ADJACENCY.md`).
- **Личный словарь пережил обновление байт-в-байт**: `personal-tt_RU-s1-f1.tpers`, md5
  `ab116194…` до и после.
- **Личный словарь жив на подписанной 1.8.3**: слово сохраняется настоящим путём (набор,
  порог три употребления; файл 95 → 122 Б), удаляется по кнопке с подтверждением
  `Forget “…”?` (122 → 95 Б), «Erase all saved words» с подтверждением очищает каталог
  `personal/` **полностью**. Pid не менялся ни на одном шаге.
- **Карантин пройден целиком, руками.** Файл обрезан со 122 до 110 байт — ровно то, что
  оставляет оборванная запись; процесс остановлен. При открытии текстового поля в **чужом**
  приложении (редактор контакта) файл переехал в `…tpers.quarantine`, а поверх редактора
  появился диалог **«Saved words could not be read, so the list is empty. A copy was kept —
  open Saved words to bring back what can still be read.»** После OK, `force-stop` и
  повторного открытия поля извещения **нет** — сказали один раз. На экране «Сохранённые
  слова» — карточка `ТАТАРЧА` / «Words that could not be read» / «**1 word** can be brought
  back. The rest of the copy is damaged and cannot be recovered.» «Bring the words back»
  вернуло слово в список и **копию не удалило** (карточка осталась и по-прежнему говорит о
  повреждении); «Delete the copy» с подтверждением убрало копию, слово осталось; «Стереть
  все слова» унесло остальное.
- **Файл-метка извещения в опрос каталога ни разу не попал** даже при шаге 100 мс: на этом
  стенде извещение показывается в том же кадре, в котором поднимается, и метка снимается
  сразу. То есть на устройстве подтверждён результат, а долговечность метки через смерть
  процесса — 22 тестами `PersonalQuarantineRecoveryTest` и прогоном миссии `tt-quarantine`.
- **Холодный старт: 259,6 / 261,7 / 262,2 / 263,0 / 268,3 / 270,5 / 271,9 / 272,0 / 276,5 /
  282,1 / 284,6 / 287,2 мс**, медиана 271,2 мс, среднее 271,6 мс при инварианте 400 мс —
  запас 112,8 мс (28,2 %) по худшему прогону. **n=12 из девятнадцати попыток**: семь
  прогонов не дали числа (после `kill -9` процесс не поднялся в отведённое время либо
  клавиатура не была показана к моменту убийства) и были отброшены, а не досчитаны. Перед
  каждым прогоном `sync; echo 3 > /proc/sys/vm/drop_caches`. Метрика та же, что в прогонах
  E2, v1.7.0…v1.8.2.
- **A/B против v1.8.2 в том же стенде**: 265,6–312,7 мс, медиана 289,4 (n=9). Медиана 1.8.3
  ниже на 18,2 мс, и диапазоны почти не перекрываются. Механизм, на который это можно
  списать, есть и он один: распакованная татарская таблица меньше на 131 810 Б, а её
  открытие лежит на пути показа клавиатуры. На выборках 12 и 9 это всё равно не
  доказательство, и отчёт его не утверждает.
- **Про `screencap` — поправка к трём прошлым прогонам.** Он на этом AVD **работает**: на
  Messages, на редакторе контакта и на клавиатуре отдаёт нормальные PNG. Нулевые файлы он
  отдаёт на **экране настроек приложения** — там, где щупали личный словарь прошлые миссии;
  отсюда и прежний вывод «не работает вообще». Там по-прежнему `uiautomator dump`.
- **Системная локаль эмулятора английская**, поэтому русский и татарский варианты
  одиннадцати строк проверены только тестами (включая наличие всех трёх переводов), а не
  глазами.
- **Место на устройстве после обновления выросло**: `files/bigrams` 644 148 → 1 156 486 Б,
  `files/bigrams-ru` 635 838 → 1 141 606 Б, итого **+1 018 106 Б**, потому что старые таблицы
  не удаляются (`MAX_FINAL_ARTIFACTS = 2`). Записано как находка, не починено.
- PSS в этом прогоне не снимался: пункт остаётся открытым, как и был.
- Не проверялось: TalkBack (на образе не установлен), поворот и альбомная ориентация,
  split-screen, физическая клавиатура, direct boot, несколько пользователей, latency как
  UX-метрика, jank, аллокации.

### Частичное свидетельство v1.8.2: эмулятор (история, прогон 2026-08-22)

Прогон кандидата до 1.8.3, тот же AVD, подписанный `dist/tatar-keyboard-1.8.2.apk` поверх
1.8.1. Личный словарь проверен целиком (сохранение, в том числе слово с `ү` из пятого ряда,
удаление по одному и последнего, «Стереть всё»); B3 — отказ удаления не убивал процесс (pid
4934 до и после), B2 — три повреждения подряд дали ровно одну карантинную копию, извещение
показано один раз, «Стереть всё» унесло и копию. Холодный старт 265,6–312,7 мс, медиана
289,4 (n=9). Скриншотов в том прогоне нет — вывод «screencap не работает» относился к экрану
настроек приложения, см. поправку выше. Подробности — `docs/archive/apk-audits/APK-AUDIT-1.8.2.md`.

### Частичное свидетельство v1.8.1: эмулятор (история, прогон 2026-08-22)

Прогон кандидата до 1.8.2, тот же AVD `tatar_e5_test`, подписанный
`dist/tatar-keyboard-1.8.1.apk` поверх 1.8.0. Оба дефекта, ради которых собрана 1.8.1,
проверены как A/B на подписанных сборках в Messages: на 1.8.0 после backspace полоса
подсказок не возвращалась и немедленный тап по ячейке не вставлял слово, на 1.8.1 обе
последовательности отработали правильно; скриншоты — `docs/archive/apk-audits/apk-audit-1.8.1/`. Холодный старт
254,9–308,3 мс, медиана 273,5 мс (n=8), A/B с переустановленной v1.8.0 — медиана 273,1 мс
(n=4). Порчу текста при зажатом backspace воспроизвести не удалось ни на 1.8.1, ни на 1.8.0 —
окно короче того, во что попадает `adb input`; пункт остаётся за проверкой на устройстве. PSS
не снимался. Ни один пункт списка выше этот прогон не закрывал.

### Частичное свидетельство v1.8.0: эмулятор (история, прогон 2026-08-22)


Прогон 2026-08-22 на AVD `tatar_e5_test` (Android 11 / API 30, x86-64, headless,
`-gpu swiftshader_indirect`, плотность 440, 1080×2280) на **подписанном release-артефакте**
`dist/tatar-keyboard-1.8.0.apk`, установленном обновлением поверх 1.7.0. Это не Samsung и
не One UI; ни один пункт списка выше не закрывается.

- **Холодный старт: 259,3 / 263,9 / 272,4 / 274,5 / 283,2 / 283,4 / 284,0 / 288,5 мс**,
  медиана 278,9 мс, среднее 276,2 мс при инварианте 400 мс — запас 111,5 мс по худшему
  прогону. Перед каждым прогоном — `sync; echo 3 > /proc/sys/vm/drop_caches`. Метрика та
  же, что в прогонах E2 и v1.7.0: старт процесса IME → первый `FrameCompleted` окна IME.
  Старт процесса берётся из `/proc/<pid>/stat` (поле 22, шаг 10 мс при `CLK_TCK=100`),
  `FrameCompleted` — из первой строки `dumpsys gfxinfo org.tatarkeyboard.ime framestats`.
  Цикл прогона: показанная клавиатура убивается `kill -9`, система пересоздаёт процесс и
  окно IME, так что старт процесса целиком относится к пути показа.
- **A/B против v1.7.0 на том же эмуляторе и в том же сеансе**: переустановленный
  `dist/tatar-keyboard-1.7.0.apk` даёт **265,2 / 271,2 / 281,6 / 283,3 / 313,2 / 323,1 мс**,
  медиана 282,5 мс, среднее 289,6 мс. Диапазоны перекрываются полностью, 1.8.0 не
  медленнее. **Третья крупная таблица данных холодный старт не удлинила.**
- Функционально на release-сборке (R8 + `shrinkResources`) проверено глазами:
  русская раскладка, `В связи ` → **с / со / и**; переключение глобусом на татарскую в том
  же процессе, `Бер мин ` → **дә / үзем / бу** (татарское предсказание не изменилось);
  поиск эмодзи при пустом запросе — каретка стоит отдельной чертой слева от подсказки
  «Поиск эмодзи»; запрос из трёх пробелов — полосы результатов нет, подсказка на месте;
  после дописанного «кот» — полоса кошачьих эмодзи; после трёх backspace поиск остаётся
  открытым с теми же тремя пробелами и без полосы. Скриншоты —
  `docs/archive/apk-audits/apk-audit-1.8.0/`.
- PSS в этом прогоне не снимался: пункт остаётся открытым, как и был. Ближайший замер —
  A/B в одном процессе миссии `tt-ru-bigrams` (`docs/archive/bigrams/RUSSIAN-BIGRAMS.md`, раздел 9):
  русская таблица прибавляет 621 КБ отображённых файлов.
- Не проверялось: TalkBack (на образе не установлен), поворот и альбомная ориентация,
  split-screen, физическая клавиатура, direct boot, несколько пользователей, latency как
  UX-метрика, jank, аллокации.

### Частичное свидетельство v1.7.0: эмулятор (история, прогон 2026-08-21)


Прогон 2026-08-21 на AVD `tatar_e5_test` (Android 11 / API 30, x86-64, headless,
`-gpu swiftshader_indirect`, плотность 440, 1080×2280) на **подписанном release-артефакте**
`dist/tatar-keyboard-1.7.0.apk`. Это не Samsung и не One UI; ни один пункт списка выше не
закрывается.

- **Холодный старт: 289,5 / 293,5 / 297,9 / 300,5 / 313,6 / 316,8 мс**, медиана 299,2 мс,
  среднее 302,0 мс при инварианте 400 мс. Перед каждым прогоном —
  `sync; echo 3 > /proc/sys/vm/drop_caches`. Метрика та же, что в прогоне E2:
  старт процесса IME → первый `FrameCompleted` окна IME. Старт процесса берётся из
  `/proc/<pid>/stat` (поле 22, шаг 10 мс при `CLK_TCK=100`), `FrameCompleted` — из первой
  строки `dumpsys gfxinfo org.tatarkeyboard.ime framestats`. Цикл прогона: показанная
  клавиатура убивается `kill -9`, система пересоздаёт процесс и окно IME, так что старт
  процесса целиком относится к пути показа.
- **A/B против v1.6.1 на том же эмуляторе и в том же цикле**: переустановленный
  `dist/tatar-keyboard-1.6.1.apk` даёт **265,0 / 282,3 / 288,2 / 301,0 / 315,4 / 327,1 мс**,
  медиана 294,6 мс, среднее 296,5 мс. Разница средних +5,5 мс, диапазоны перекрываются
  полностью. **Второй словарь холодный старт не удлинил.**
- Функционально на release-сборке (R8 + `shrinkResources`) проверено глазами:
  русская раскладка, «Слов» → **Словам / Слова / Слово**; «ещ» → **Еще / Ещё / Ещенко**
  (обе орфографии, как и обещает changelog); переключение глобусом обратно на татарскую в
  том же процессе — «бер» → **Беренче / Берсе / Берничә**. Скриншоты —
  `docs/archive/apk-audits/apk-audit-1.7.0/`.
- Total PSS показанной клавиатуры с обоими прогретыми движками — **35 591 КБ**
  (`dumpsys meminfo`, одиночный замер, подсказки ВКЛ). Абсолютное число на программном
  рендерере завышено и в приёмку не входит; плеча ВЫКЛ в этом прогоне не снималось,
  поэтому дельта не считается и пункт PSS остаётся открытым.
- Не проверялось: TalkBack, поворот и альбомная ориентация, split-screen, физическая
  клавиатура, direct boot, несколько пользователей, latency как UX-метрика, jank,
  аллокации.

### Частичное свидетельство E2: эмулятор (не закрывает ни один пункт выше)

Прогон на AVD `tatar_keyboard_d1f_api35_arm64` (Pixel 3a, Android 15 / API 35,
google_apis arm64, headless, cold boot, `-gpu swiftshader_indirect`). Это не Samsung и не
One UI: у One UI своя оболочка IME-хостинга, свои insets, свой шрифт/плотность и свой
переключатель раскладок.

Устанавливалась **предыдущая** сборка `dist/tatar-keyboard-1.2.0.apk` — до автопробела
(`bacf177`), а не артефакт из evidence-таблицы выше. Поэтому автопробел не покрыт этим
прогоном вообще. Коммит `bacf177` затронул только путь коммита по тапу
(`InputLogic.commitChosenSuggestion`, `LatinIME.commitSuggestion`) и чистый предикат
`TatarWordUtils`: измерения вне тапа (высота полосы, insets, холодный старт, PSS, jank,
privacy-гейтинг) продолжают описывать текущую сборку, функциональные результаты по тапу —
уже нет.

- Функционально зелёное: обычный ввод не регрессировал, opt-in по умолчанию OFF, 0–3
  результата, тап коммитит после переключения ru→tt в уже открытом поле, заглавная даёт
  и вставляет заглавные формы, при курсоре в середине слова полоса пуста, свайп-удаление
  и слайд по пробелу гасят подсказки, в поле пароля полоса `GONE`. Крэшей, ANR и
  tombstone нет.
- Высота полосы: `contentTopInsets` 1294 (ВЫКЛ) против 1184 (ВКЛ) = 110 px = 40 dp при
  density 440.
- Холодный старт 124–147 мс (один прогон с принудительным `drop_caches` — 143 мс) при
  бюджете 400 мс; метрика — `am_proc_start` → первый `FrameCompleted` окна IME.
- **PSS-бюджет провален:** 33,4–33,6 МБ с включённой D1 против 29,2–29,4 МБ с
  выключенной при бюджете 30 МБ (пик сразу после показа до 38,9 МБ). Чистый A/B, где
  различается только `pref_tatar_suggestions`: цена фичи ≈ +4,2 МБ PSS (+1,9 МБ native
  heap). Абсолютные числа на программном рендерере завышены, достоверна дельта; нужен
  перезамер на реальном железе. Сноска: бюджет 30 МБ отменён, см. `docs/archive/PROPOSALS.md`, раздел
  «Бюджет памяти (PSS)». Текст измерения оставлен как есть — он описывает прошлое
  состояние, и переписывание сделало бы свидетельство неверным.
- Jank 25,93% при Slow UI thread = 0 и кадрах 16–19 мс — профиль софтверного GPU; бюджет
  «janky ≤ 1%» ни подтверждён, ни опровергнут (NOT_COVERED).
- Не проверялось: TalkBack и реальный обход virtual nodes, поворот и альбомная
  ориентация, split-screen, физическая клавиатура, direct boot, несколько пользователей,
  тёмная тема, другие плотности, смена системной локали, latency как UX-метрика (ввод
  подавался через `adb input tap`).
- Отсутствие полосы в поле пароля подтверждено метрически (`contentTopInsets` = 1294 плюс
  uiautomator `password=true`); визуального скриншота нет из-за `FLAG_SECURE`.

Полная матрица D1f описана в `docs/archive/PROPOSALS.md` и фазовых документах
`docs/archive/dictionary/DICTIONARY-D1B.md`–`docs/archive/dictionary/DICTIONARY-D1E.md`.

## 4. Финальный distributable

Только после шагов 1–3 атомарно опубликовать проверенный APK в `dist/` и проверить, что
копия побайтово совпадает с уже проаудированным build output.

Фактически: копия сделана после шага 2, когда шаги 1 и 3 ещё открыты. Файл в `dist/` —
локальный аудируемый артефакт для device-UAT, а не готовый к публикации релиз. После
закрытия шагов 1 и 3 сборка и копия должны быть повторены с frozen commit.

**Пометка 2026-08-23: шаг 4 выполнен заново на кандидате v1.8.3.** Копия сделана из
подписанного `app-release.apk` сборки 2026-08-23 (v1.8.3) командным блоком ниже. Полный
цикл сборки здесь был **один**, и копия сделана из его вывода. Числа в трёх отметках ниже —
свежие; всё, что относится к прогонам v1.8.2 и старше и к утраченному артефакту от
2026-07-25, помечено как историческое. Подробности — `docs/archive/apk-audits/APK-AUDIT-1.8.3.md`.

Рядом в `dist/` остаются `tatar-keyboard-1.2.0.apk` (2026-08-18),
`tatar-keyboard-1.3.0.apk` (2026-08-19), `tatar-keyboard-1.4.0.apk`,
`tatar-keyboard-1.5.0.apk`, `tatar-keyboard-1.6.0.apk` и `tatar-keyboard-1.6.1.apk` (все
2026-08-20), `tatar-keyboard-1.7.0.apk` (2026-08-21), `tatar-keyboard-1.8.0.apk` и
`tatar-keyboard-1.8.1.apk` (оба 2026-08-22), `tatar-keyboard-1.8.2.apk` (2026-08-22) —
артефакты прошлых аудитов. Они не удалены и **кандидатами на публикацию не являются**:
кандидат — `dist/tatar-keyboard-1.8.3.apk`. Впервые за четыре релиза его размер заметно
отличается от предыдущего — 2 491 557 Б против 2 582 745 Б у 1.8.2, — но три файла до этого
(1.8.0, 1.8.1, 1.8.2) по размеру практически не различаются, поэтому правило прежнее:
**различать по имени файла и обязательно сверять SHA-256**; `aapt2 dump badging` даёт
versionCode.

**Пометка 2026-07-26: этой копии больше нет.** Абзац и три отметки ниже описывают
состояние на 2026-07-25 и оставлены как история. Каталог `dist/` пуст, командный блок ниже
без новой сборки выполнить нельзя, а отметки `[x]` к будущему артефакту не относятся: шаг 4
выполняется заново после пересборки, и его числа заполняются с нуля. Подробности и причина
— раздел «Пробел в свидетельствах» в начале файла.

```sh
cp app/build/outputs/apk/release/app-release.apk dist/tatar-keyboard-1.8.3.apk
cmp app/build/outputs/apk/release/app-release.apk dist/tatar-keyboard-1.8.3.apk
sha256sum dist/tatar-keyboard-1.8.3.apk
apksigner verify --verbose --print-certs dist/tatar-keyboard-1.8.3.apk
```

- [x] `dist/tatar-keyboard-1.8.3.apk` совпадает с audited build output — `cmp` с
  `app/build/outputs/apk/release/app-release.apk` без различий, оба **2 491 557** байт,
  SHA-256 обоих `a5b1279357f0f8e0f03ba746ed88a750bcd3e369a23e1e5555ce6c2901fdb85c`,
  `apksigner verify` → `Verifies` (v2 only, сертификат `98ca6feb…`),
  `aapt2 dump badging` артефакта — `versionCode='14' versionName='1.8.3'`. Прогон 2026-08-23.
  Историческая запись прогона 2026-08-22 (артефакт v1.8.2): 2 582 745 Б, SHA-256
  `6de28e5d…`, `versionCode='13' versionName='1.8.2'` — сумма сверена заново 2026-08-23 и
  совпала; артефакт v1.8.1: 2 578 765 Б, SHA-256 `93ca7854…`; артефакт v1.8.0: 2 578 753 Б,
  SHA-256 `e7d99887…`.
- [x] SHA-256 и все поля evidence выше обновлены после копирования. **Frozen commit на этой
  версии есть** — `bcc439192d89e4e275ed56bdd7d784056c05cde4`, создан локально до сборки, он и
  стоит в таблице. Коммиты после него (этот отчёт, ретаргет чеклиста, один пункт в
  `CHANGELOG.md`) в APK не попадают и артефакт не меняют.
- [x] Повторная сборка после этого шага не выполнялась: mtime build output — 2026-08-23
  21:16:53, mtime копии в `dist/` — 21:17:35, копия позже сборки, содержимое побайтово
  идентично (`cmp` exit 0); mtime `app-release.apk` остался 21:16:53 и после всех
  последующих прогонов, проверок и часа работы на эмуляторе. Любая новая сборка обязывает
  повторить artifact audit и публикацию в `dist/`: сборка не байт-в-байт воспроизводима, тот
  же коммит даёт другой SHA-256.
  Историческая запись прогона 2026-08-22 (артефакт v1.8.2): mtime build output —
  17:01:33, копии — 17:04:47, SHA-256 обоих —
  `6de28e5da06621ac814ec2eb2b6cfd7bbfe14a81540ad1cc1de8315e57e99d0f`.
  Историческая запись прогона 2026-08-22 (артефакт v1.8.1): mtime build output —
  14:34:03, копии — 14:34:15, SHA-256 обоих —
  `93ca78547fddd5a803199f8320610b2e3516dc6400eec6a3e11cdc59f0a3b156`.
  Историческая запись прогона 2026-08-21 (артефакт v1.7.0): mtime build output —
  03:57:02, копии — 03:57:36, SHA-256 обоих —
  `9e670053176907e410cf9528e92bb496a672f08f8d597db91812d3df41af3a62`.
  Историческая запись прогона 2026-08-20 (артефакт v1.6.1): mtime build output —
  16:37:22, копии — 16:37:41, SHA-256 обоих —
  `c48246ab4301848d71c02c116c3e235708e47a5f75013c67d8e914fb60d0fac5`.
  Историческая запись прогона 2026-08-20 (артефакт v1.6.0): mtime build output —
  15:46:40, копии — 15:47:32, SHA-256 обоих —
  `df8da11e9767732d8186423a28cf0249210735012298136eddfdd502280d180a`.
  Историческая запись прогона 2026-08-20 (артефакт v1.5.0): mtime build output —
  04:22:36, копии — 04:22:59, SHA-256 обоих —
  `29902121835feb0a3bfa659f22557db8107732422d8e37766dfbd5ea56d1cb83`.
  Историческая запись прогона 2026-08-20 (артефакт v1.4.0): mtime build output —
  02:49:20, копии — 02:49:53, SHA-256 обоих —
  `fea08bdd76e74c091e009ba73f18f6a3eac09a3574c9f256786717c5c218113c`.
  Историческая запись прогона 2026-08-19 (артефакт v1.3.0): mtime build output —
  12:53:52, копии — 12:54:12, SHA-256 обоих —
  `9f366d6cddfdb172fbd254a27afd91f238e51ac94bc34e5531040521cdaa74ea`.
  Историческая запись прогона 2026-08-18 (артефакт под номером 1.2.0): mtime build output —
  16:46:39, копии — 16:47:14, SHA-256 обоих —
  `18fc03695ec6421c745e536abda32c7cdf2d8779acecaae095d5a5e561a77256`.
  Историческая запись прогона 2026-07-25 (утраченный артефакт): mtime build output и копии
  совпадали — 02:02:38, позже коммита `c3ed443` (02:02:24), SHA-256 обоих —
  `26afd03f200f2939e5ce3b5f102bf4dcd93b5fbb8635161cd393b941cff13bcf`.

## 5. Commit, push и CI

Версионные литералы в разделах 5–7 перецелены на v1.8.3 вместе со всем файлом (2026-08-23);
существо и статус пунктов не менялись — ни одна отметка не переведена в `[x]`. Разделы 5–7
закрывает оператор руками: это публикация. **Ни push, ни тега, ни релиза этой миссией не
делалось** — на v1.8.3 есть только локальный release commit `bcc4391`.

- [ ] Release-status commit содержит только проверенные исходники, metadata и
  документацию; APK, keystore, passwords и локальные конфиги не попали в git.
- [ ] `d1-sequential` запушена по SSH; remote SHA совпадает с локальным.
- [ ] После merge целевая `main` указывает на audited frozen commit.
- [ ] Анонимно открываются репозиторий, `LICENSE`, `PRIVACY.md` и raw-файлы. Текущий
  HTTP 404 означает, что этот gate пока открыт.
- [ ] GitHub Actions для frozen commit зелёный и явно содержит unit tests,
  `lintVitalRelease`, build, APK-size и no-INTERNET gates. Локальный pass не считается
  подтверждением Actions.
- [ ] Негативная no-INTERNET ветка/commit действительно даёт красный CI именно на
  permission gate; после проверки она удалена и не слита.

## 6. Tag и GitHub Release

Не создавать tag/Release, пока шаги 1–5 не закрыты и GitHub не доступен анонимно.

```sh
git tag -a v1.8.3 -m "Tatar Keyboard 1.8.3"
git push origin v1.8.3
```

- [ ] Tag `v1.8.3` существует на remote и указывает на audited frozen commit
  `bcc439192d89e4e275ed56bdd7d784056c05cde4`.
- [ ] GitHub Release имеет title `Tatar Keyboard 1.8.3`, release notes соответствуют
  `[1.8.3]` в CHANGELOG и содержит только `tatar-keyboard-1.8.3.apk` — **именно файл из
  `dist/`** с SHA-256 `a5b1279357f0f8e0f03ba746ed88a750bcd3e369a23e1e5555ce6c2901fdb85c`, а
  не пересобранный: тот же коммит даёт другой SHA-256.
- [ ] APK скачан из Release без авторизации; его SHA-256, подпись, version и permissions
  повторно совпали с локальным evidence.

## 7. IzzyOnDroid — актуальный процесс Codeberg

Старая инструкция через GitLab `IzzyOnDroid/repo` больше не используется. После
публичного GitHub Release открыть
https://codeberg.org/IzzyOnDroid/repodata/issues/new/choose и выбрать точный шаблон
**App Inclusion Request** (`.forgejo/issue_template/app-inclusion-request.yaml`).

В форме указать source URL, Apache-2.0 для кода, отдельную CC BY 4.0 атрибуцию данных
словаря, категорию Writing, описание, CLI build instructions, Fastlane metadata и ссылку
на публичный v1.8.3 Release. До подачи отдельно сверить актуальную
[App Inclusion Policy](https://izzyondroid.org/docs/general/AppInclusionPolicy/).

Требования каталога к тексту заявки оператор хранит локально, вне репозитория.

- [ ] Inclusion request создаётся только после публичного Release; текст заявки — по
  локальным требованиям оператора, вне репозитория.
- [ ] После фактического включения приложение найдено в IzzyOnDroid, установка и update
  подписанным APK проверены.
- [ ] Только после включения добавлен рабочий IzzyOnDroid badge и закрыты внешние
  publication requirements.

## Историческая справка v1.1.0

Проверенный v1.1.0 APK имел размер 832 442 байта, валидную v2-подпись и SHA-256
`071dcbc4ef513f208a4a77b4a600790f90d2f731067c5c4ce11f54d06046ff21`.
Его build, UAT или waiver не закрывают ни один checkbox v1.5.0.

**Пометка 2026-08-19 (действует и для v1.5.0): как эталон сертификата эта справка больше
не действует.** Сертификат
v1.1.0 `cdd8c5350ddc86f13cd89b5bfb55ca33c13efba77beb4d4ccb75d5e6b961b09e` перестал быть
релизным 2026-08-18, когда решением оператора был сгенерирован новый ключ. Действующий
релизный сертификат — `98ca6febfed6c146d81c1fdcfe52c79acf7aa926a1033d98b844a59803ec42ad`
(RSA 4096, `CN=Tatar Keyboard`), им подписаны артефакты 2026-08-18, v1.3.0, v1.4.0 и v1.5.0. Сверять
подпись кандидата со старым отпечатком нельзя: сравнение заведомо не пройдёт, upgrade path
с v1.1.0 разорван намеренно. Подробности — «Подписная идентичность» в
`docs/archive/apk-audits/APK-AUDIT-1.4.0.md`.
