# Документация tatar-keyboard — индекс

Живые документы лежат наверху `docs/`, отчёты завершённых миссий — в
`docs/archive/` по тематическим категориям. Архив не редактируется
(историческая запись); актуальное состояние проекта — в корневом `HANDOFF.md`.

## Живые документы

- `ROADMAP.md` — план оставшихся работ (предсказания, UX/дизайн, техдолг),
  7 фаз после релиза 2.0.1; 2026-09-22.
- `ROADMAP-P1.md` — отчёт фазы 1 целиком, 2026-09-22: движковая часть
  (P3a/P3b/P4/T5), гигиена (T3/T4/T6), финальный блок (T1 — baseline-профиль,
  гейты, UAT на POCO C71 13/13 PASS, холодный старт 257/270 мс).
- `ROADMAP-P2.md` — отчёт фазы 2 по пунктам, 2026-09-23: P1 (личные биграммы —
  формат TPERSB1, обучение с порогом 2, ранжирование NEXT_WORD, гейты), U7 (экран
  личного словаря: слова и пары по языкам, счётчики использований, удаление и
  очистка, карантинные карточки обоих хранилищ), U8 (режим инкогнито — пауза
  обучения обоих хранилищ) — канонический английский. Финальный блок тем же днём:
  PRIVACY.md 1.5 (оба хранилища, pending-хеши, соли, карантины, семантика
  инкогнито), все гейты зелёные, UAT на POCO C71 — обучение пары и удаление
  доказаны на железе; телефон отключился физически посреди цикла (USB), остаток
  (часть инкогнито, регрессия, холодный старт) заблокирован и ждёт возврата
  устройства.
- `ROADMAP-P3.md` — отчёт фазы 3 по пунктам (канонический английский). P2
  2026-09-23: визуальный контракт автозамены (AOSP) — превью замены в полосе
  подсказок во время набора (typed-ячейка «оставить как есть» + подчёркнутая
  жирная акцентная замена), машина состояний и подавление по слову, +24 JVM-теста
  (1400 → 1424), все гейты зелёные. P7 тем же днём: расширение классов
  автозамены ОТКЛОНЕНО своими же гейтами с числами (G1 — 3 плохих + 1 опасный
  случай из 5; G2 — лифт +0,57 п.п. при пороге +5 п.п.), классы остаются {1}/411.
  Финальный блок: все гейты зелёные; UAT на POCO C71 заблокирован (телефон
  физически отключён весь сеанс) — вся машина превью доказана на эмуляторе
  скриншотами (8/8), девайсные строки ждут возврата устройства.
- `ROADMAP-P4.md` — отчёт фазы 4 по батчам (канонический английский). Batch A
  2026-09-23: перепаковка таблицы биграмм (K=3, 13 154 головы). Batch B тем же
  днём: P6 (восстановление двух правок, класс 5) — ОТКЛОНЁН своими же гейтами с
  числами: потолок recovery@3 при идеальном покрытии 9,79 % ниже порога +10 п.п.
  (медианный ранг исходного слова 28 среди ~114 конкурентов на строку), а
  цепочечное перечисление с пробами пробивает fail-closed бюджет на 73 % строк
  срабатывания; класс остаётся в дереве unwired, калибровка и числа запинены как
  свидетельство; G2/G3 (структура, хост) зелёные, девайсный замер — финальный
  батч на POCO C71 (не подключён). Финальный блок тем же днём: все гейты
  зелёные (JVM 1 443/0, python 484/0, линт, пины ассетов 16/16 с новой
  таблицей, release_check --quick 8/8); UAT на устройстве заблокирован
  (отключён весь сеанс) — эмуляторный фолбэк: новая голова автобуска →
  утырып·кереп(+🚌 хвост), старые головы и весь регрессионный ряд в норме.
- `ROADMAP-P5.md` — отчёт фазы 5 по пунктам (канонический английский). U6
  2026-09-23: настройка «Высота клавиатуры» — три именованных пресета
  (Compact 0.85 / Default 1.00 / Tall 1.15) вместо унаследованного ползунка
  50–150 %; то же хранилище (`pref_keyboard_height`, float), наследственные
  значения продолжают работать и показываются процентом; live-apply через
  mHeight в KeyboardId; +13 JVM-тестов (1443 → 1456). U1–U5 — девайсная
  валидация финальным батчем фазы. Финальный батч 2026-09-24 (POCO C71): все
  гейты зелёные; U6 на железе — пресеты Compact/Default/Tall рендерятся с
  выравниванием эмодзи-панели (верх клавиатуры y≈1053/980/908); U1 TalkBack —
  механика на месте (виртуальные узлы клавиш, анонсы полосы), слуховая проверка
  остаётся за человеком; U2 Direct Boot — статически чисто (DE/CE разводка,
  гейты разблокировки), живая перезагрузка — за оператором; U3 Telegram —
  BLOCKED (не установлен); U4 — HyperOS игнорирует navigation_mode из adb,
  клавиатура корректна в доступном режиме; U5 — нет планшета, BLOCKED;
  регрессии 2-й фазы (инкогнито глубокая проверка + забыть тестовую пару)
  закрыты; регрессионное ядро, холодный старт 253/264 мс, crash-буфер пуст.
- `ROADMAP-P6.md` — отчёт фазы 6 (фундамент, T2), канонический английский.
  Часть 1/3, 2026-09-23: разбиение SuggestionsController.kt (2 525 → 2 000
  строк) чистыми переносами — шесть новых файлов (SuggestionSurfaces,
  SuggestionPreparation, LanguageSlot, CleanRunMachine, RevertWindow,
  AutocorrectPreview), счётчик тестов неизменен (1 456) после каждого шага,
  задокументированное исключение из цели ≤1 500 с раскладкой остатка.
  Часть 2/3, 2026-09-23: разбиение LatinIME.java (2 440 → 2 254 строки) —
  четыре новых файла (LatinImeAutocorrect, LatinImeKeyFeedback,
  LatinImeSoftInputWindow, LatinImeEmojiSearch), 20 контрактных тестов,
  читающих файл, сняты заранее и все остались зелёными; исключение из цели
  с количественной раскладкой запиненного остатка. Часть 3/3 тем же днём:
  SettingsHostActivity (1 568 → 1 129) и EmojiPanelView (1 317 → 1 165).
  Финальный батч 2026-09-24: baseline-профиль регенерирован (2 433 → 2 561
  правило, извлечённые классы горячие), все гейты зелёные (JVM ровно 1 456),
  UAT на устройстве заблокирован активным использованием телефона хозяином —
  эмуляторный прогон 12/12 PASS доказал нулевую дельту поведения.
- `GLIDE-PLAN.md` — утверждённый план фазы 7 (glide/swipe-набор): алгоритм
  (двухканальный статистический классификатор SHARK² по мотивам FlorisBoard /
  AnySoftKeyboard PR #1870), фазы P7-1…P7-4 с письменными гейтами.
- `ROADMAP-P7.md` — отчёт фазы 7 (канонический английский). P7-1 2026-09-24:
  ядро glide-декодера (пакет `latin/glide/`, Android-free, ноль аллокаций в
  машинерии) + офлайн-калибровка на синтетических жестах поверх реального
  татарского словаря: генератор `scripts/glide_pack.py` (целочисленно-точная
  модель шума, побайтовая зеркальность JVM↔python по SHA-256), гейты G1/G2/G3
  зелёные на held-out (top-1 69,94 %, top-3 77,57 %, p95 1,19 мс на хосте),
  индекс слов ≈ 3,0 МиБ; константы настроены на train-сплите, отклонения от
  референсных σ задокументированы с числами. P7-2 тем же днём: тач-интеграция —
  `GlideGestureDecider` (пороги FlorisBoard: ширина клавиши, 0,10 dp/мс, окно
  500 мс), хирургия PointerTracker (armed-глайд коротко замыкает ветки
  space/delete, исторические батчи читаются, мультитач отменяет глайд
  fail-closed), `onGlideInput` — default no-op, в LatinIME задокументированная
  заглушка P7-3; преф `pref_glide_typing` (ON, подчинён подсказкам) читается с
  P7-2; контрактные тесты пинят структуру ветвления и побайтовую неизменность
  пути при выключенном префе. P7-3 тем же днём: движок + полоса + настройки —
  `LookupKind.GLIDE` с той же токен-дисциплиной (снапшот пути в запросе),
  `GlideDecoderHost` (ленивый индекс на воркере движка, геометрия volatile-пушем),
  третья привязка полосы (`displayedGlideContext`), коммит через
  predicted-word путь с авто-пробелом и цепочкой NEXT_WORD, регистр по шифту
  через seam, строка настроек с тремя локалями; e2e-пины на реальных ассетах:
  глайд сәләм → top-3 → коммит → цепочка сәләмә·һәм·белән; русская раскладка
  тоже; личный словарь не участвует (задокументировано). P7-4 тем же днём: все
  гейты зелёные (JVM 1 534/0, python 505/0); девайсный замер на POCO C71 —
  инструментальный тест `GlideDeviceInstrumentationTest`: сәләм в top-8 на живой
  геометрии, но **латентность p95 50,03 мс против записанного гейта ≤ 5 мс**
  (стабильно, без GC; хост 1,19 мс — телефонное соотношение ~40× согласовано с
  E3b) — число записано, решение за оператором; интерактивный UAT на устройстве
  заблокирован активным использованием телефона хозяином, эмулятор нёс доказательство
  (9/9 PASS: строка настроек ON по умолчанию, жест сәләм → top-3 → коммит → цепочка,
  ru работа в ячейке 1, тач-набор нетронут, space-свайп жив, при OFF — legacy
  поведение, задокументированное отсутствие трейла до отрыва пальца).
- `APPLE-UX-2026-09-25.md` — аудит текущего состояния UX/UI против iOS-референсов
  и план сближения (канонический английский): что уже совпадает, 11 расхождений
  с file:line, дешёвые правки W1–W6, средние M1–M5, структурные S1–S2, список
  того, что копировать у Apple НЕ надо (SF Pro/SF Symbols, Liquid Glass,
  iOS 26 единый цвет клавиш), рекомендованный первый батч.
- `ROADMAP-P8.md` — отчёт фазы 8 (канонический английский), 2026-09-25: стадия R (релиз
  накопленной волны 3.1.0/37 — гейты, версия, changelog, pack x2, release_check 16/16),
  A (батч Apple-UX: тень, серый, метка пробела, системная вибрация, полировка полосы,
  три состояния Shift), B (все семь пунктов: анимация экранов, акцентный Enter, цвета панели
  альтернатив, полоса 44dp, отклик только баллоном — с обоснованным отклонением от плана,
  баллон-капля, стиль диалогов), C (объяснение выключенных строк, один резидентный glide-индекс,
  отказ от шардирования эмодзи с числами и закрытие пика загрузки, вердикты по хвостам свайпа),
  D (supply-chain: SHA-пины экшенов, пин build-tools, verification-metadata, упаковка под
  CI-гейтом воспроизводимости, устранение дельты gradlew), F (гигиена документов).
- `ROADMAP-P8-PLAN.md` — утверждённый план фазы 8 (канонический английский):
  стадия R — выпуск накопленной волны (гейты на текущем дереве, разбивка
  коммитов, 3.1.0/37, pack + release_check, тег), A — первый батч Apple-UX
  (W1/W2/W3/W6/W4/M1), B — пункты Apple-UX под решение оператора (реактивация
  отменённой волны K), C — инженерный бэклог (F15a, шардирование эмодзи-индекса,
  общий glide-индекс, четырёхклеточная полоса), D — supply-chain (T5–T8, T10),
  E — девайсный UAT, F — гигиена документов; реестр решений оператора из 5 пунктов.
- `PUBLISH-CHECKLIST.md` — чек-лист публикации релиза (перецеливается на текущую версию).
- `CLEANUP.md` — журнал очистки документации; свидетельства в `cleanup/`.
- `RESTRUCTURE.md` — журнал кампании аудита/реструктуризации/оптимизации (2026-08-30).
- `RESTRUCTURE-PLAN.md` — утверждённый план той же кампании.
- `APK-AUDIT-3.1.0.md` — аудит артефакта текущего релиза (3.1.0: волна
  оптимизации + фиксы аудита, 182 записи архива, arsc сжат, данные побайтно
  прежние); `APK-AUDIT-3.0.2.md`,
  `APK-AUDIT-3.0.1.md`, `APK-AUDIT-3.0.0.md`,
  `APK-AUDIT-2.0.1.md`, `APK-AUDIT-2.0.0.md`, `APK-AUDIT-1.9.15.md`,
  `APK-AUDIT-1.9.14.md`, `APK-AUDIT-1.9.13.md`, `APK-AUDIT-1.9.12.md`, `APK-AUDIT-1.9.11.md`, `APK-AUDIT-1.9.10.md`, `APK-AUDIT-1.9.9.md`,
  `APK-AUDIT-1.9.8.md`,
  `APK-AUDIT-1.9.7.md`, `APK-AUDIT-1.9.6.md`, `APK-AUDIT-1.9.5.md` — предыдущих;
  аудиты более ранних версий — в `archive/apk-audits/`.
- `DEV-PLAN.md` — план улучшений разработки (tooling, тесты, CI; без продуктовых
  фич), 2026-08-31. **Все 7 пунктов выполнены 2026-08-31.**
- `AUDIT-2026-08-31.md` — полный аудит (безопасность, UX, оптимизация) с
  находками и рекомендуемым порядком закрытия. **Все находки закрыты
  2026-08-31** (датированные пометки внутри), итог — релиз-кандидат 1.9.6.
- `AUDIT-2026-09-24.md` — консолидированный аудит релиза 3.0.0 по пяти углам
  (безопасность, производительность, корректность, данные/лицензии, UI/UX),
  HEAD `b8de3916`; канонический английский. Релизный блокер H1 (ссылки
  приватности/лицензии на старый репозиторий) и ряд MINOR закрыты двумя
  волнами правок (F1 — код, DOCS/DATA/STRINGS — документы, ресурсы, строки);
  остальное — documented/accepted-risk с таблицей находок и списком
  остаточных рисков.
- `SECURITY-AUDIT-2026-09-25.md` — консолидированный аудит следующего раунда
  (2026-09-25, канонический английский) по пяти углам (новый код, приватность,
  живучесть, пайплайн, UI): закрыты жизненный цикл кэша текста редактора
  (очистка на onFinishInput/onWindowHidden), полный запрет перечитывания
  парольных полей, правило вставки в CleanRunMachine (первое наблюдение
  ≤ 2 UTF-16 units), FLAG_SECURE на диалогах с личным содержимым, пайплайн
  (мульти-подпись в release_check, set-equality ассетов dictionaries/bigrams,
  distributionSha256Sum, запрет аплоада артефактов из fork-PR, защита OUT от
  симлинков и полная позаписная сверка zip в release_pack); F1–F17 живучести —
  параллельная волна; остальное — accepted/documented с таблицами.
- `OPTIMIZE-2026-09-25.md` — SAFE-волна оптимизаций (2026-09-25, канонический
  английский): мёртвые ресурсы, contains()-гейт в readKeyboardColor,
  регенерация startup-профиля (появились LatinIme*/glide-правила), убран
  двойной binary search и двойной rebuild полосы подсказок, дедупликация
  requestCurrentPrefix, lazy GlidePath, каденс фейда трейла 33 мс, стоп
  флинга эмодзи-панели при скрытии, уборка за long-press и маркерными
  таймерами; новый p95-пин композитного NEXT_WORD; отложенное — с оценками.
- `SECURITY-AUDIT-2026-09-25-FIXES.md` — половина волны правок аудита
  2026-09-25 (сам аудит — `SECURITY-AUDIT-2026-09-25.md`, его ведёт
  параллельный агент): устойчивость ввода — порог вставки 64 Ki chars (F1),
  липкий mCursorMoved (F2), ограничение кэша перед курсором (F3), валидация
  SurroundingText (F4), try/finally у всех batch-правок (F5), гарды мёртвого
  редактора (F6), нормализация инвертированного выделения (F7), применение
  фоновой перечитки кэша на UI-потоке (F8) и её коалесцирование (F10),
  граница ZWJ и гигиена getUnicodeSteps (F11), дедуп/эвикт в
  PointerTrackerQueue (F13), отсев нефинитных точек глайда (F14), кап
  записей эмодзи-подсказок (F16), NaN-гард ячеек полосы (F17).
  Канонический английский, пины и номера гейтов внутри.
- `SIZE-OPTIMIZATION-RESEARCH.md` — ресерч ужатия APK без потерь (2026-09-01):
  измеренный потенциал −29 % сменой бинарной схемы словарей/биграмм; с датированной
  финальной пометкой о фактическом результате (−15,3 %).
- `EMOJI-SUGGEST-RESEARCH.md` — ресерч эмодзи-подсказок в полосе подсказок
  (2026-09-01): вердикт — вариант A (append на завершённом слове), курируемые
  данные, отдельный тоггл.
- `EMOJI-SUGGEST-PLAN.md` — план реализации эмодзи-подсказок (3 миссии).
  **Все три миссии выполнены 2026-09-01** — релиз 1.9.10.
- `FINAL-AUDIT-2026-09-02.md` — финальный предрелизный аудит 1.9.11
  (безопасность, баги, производительность, полнота): единый реестр долгов.
  **Всё закрыто 2026-09-02**, кроме операторских (A2 публикация, C8 jks) и
  железных (D1/D2) пунктов — релиз 1.9.12.
- `DEVICE-UAT-1.9.12.md` — первая полная проверка на живом устройстве
  (2026-09-04, POCO C71 / Android 15 Go): холодный старт (медиана 300,6 мс),
  PSS по фазам, матрица из 36 сценариев, все гейты репозитория. Закрывает D1 и
  большую часть D2. Два дефекта, оба закрыты 2026-09-04: Д-1 (панель эмодзи под
  навбаром) и Д-2 (нет Enter на планшетах). Свидетельства — `device-uat/evidence/`.
- `ERRORPRONE-TRIAGE.md` — разбор всех 64 находок error-prone (2026-09-05):
  закрыто 5, среди них настоящий латентный NPE в загрузке управляемых политик
  (ограничение цвета не-строкового типа роняло весь разбор). Остальные 59
  разобраны по классам с обоснованием, почему остаются.
- `TT-SUGGESTIONS-PLAN.md` — approved plan of the Tatar prediction mission
  (P0–P5): pinned eval harness, build-time paradigm generator, dictionary
  expansion, runtime word-form suggestions, sentence-start predictions.
  **All six phases done 2026-09-19…20.**
- `TT-SUGGESTIONS.md` — mission report for the same (2026-09-19…20): baselines,
  generator validation (kaikki recall 90.34 %), Tatar dictionary 100 000 →
  110 000 entries, same-stem boost 57.58 → 62.82 %, strip-empty at sentence
  start 100 → 0 %, final gates and emulator smoke. Uncommitted — awaits the
  operator's commit/release decision.
- `TT-TYPO-NEXT-PLAN.md` — approved plan of the typo-recovery / tap-followup
  mission (phases A–D): predictions right after an accepted suggestion, then
  calibrated typo recovery in the strip.
- `TT-TYPO-NEXT.md` — mission report for the same. Phase A done 2026-09-20:
  the strip shows next-word predictions immediately after a suggestion tap
  (a tap-commit now issues the follow-up lookup itself); the D1-era empty-band
  pin was amended to the E5 contract; emulator smoke gained the
  `tap-followup-tt-сакчы` probe. Phase B measured 2026-09-20, verdict NOT
  SHIPPED: the device-true geometry model (offline == live POCO C71 dump,
  32/32 pairs), the per-engine `FuzzyEditPolicy` seam and the same-length bonus
  landed as tested infrastructure, but gate G1 (recovery lift 0.909×/1.002× vs
  the required 1.5×) and gate G2 (pollution 7.93 % vs 2 %) failed, so edit
  class #2 stays off and every engine runs the pre-Phase-B default. Phase C
  measured the same day: the probe-first full single-substitution class #4
  ({1,4}+bonus, gated on an empty exact pass at ≥ 4 cp) failed its first
  measurement round; Phase C2 (orchestrator amendment — same-set lift gate,
  activation-rate cap, device budget) re-engineered the probe path
  (no-cache search + per-position range narrowing, device 31.6 → 3.3 ms p95)
  and ALL corrected gates passed: **class #4 ships in the Tatar engine and
  `сцләм` offers `сәләм` in cell 1 by the 5th letter**; Russian untouched.
  Phase D done 2026-09-20: full gates green, signed release APK (1 849 555 B,
  SHA-256 `32cd873b…`), device UAT on the POCO C71 16/16 PASS (tap→predictions,
  the сцләм ladder, no-pollution, cold start 253/265 ms median, clean crash
  buffer). Uncommitted — awaits the operator's commit/release decision.
- `TT-NEXTWORD-FILL-PLAN.md` — approved plan of the never-empty-strip mission
  (phases A–E, post-2.0.0): after a committed word, cells left free by bigram
  successors and word forms fill with the language's global top-frequency words.
- `TT-NEXTWORD-FILL.md` — mission report for the same. Phases A–C done
  2026-09-20: `TdictPrefixIndex.topFrequentWords`, the `FallbackWords` seam
  (order bigrams > forms > fallback; never displaces; committed word excluded;
  emoji tail kept), eval strip-empty-after-word 33.30 % → 0. Phase D done the
  same day: all gates green, signed APK 1 849 555 B (SHA-256 `d210f0c9…`) —
  device UAT BLOCKED (the POCO C71 was physically disconnected all session; the
  operator's scenario was replayed on the emulator instead, 12 rows PASS,
  including the finding that the literal `сэлэм` with two э cannot offer сәләм
  by the single-edit design). Device replay and Phase E (independent
  re-verification) pending. Uncommitted — awaits the operator's decision.
- `RESEARCH-FIXES.md` — миссия починки находок ресерча (2026-09-04, релиз 1.9.14):
  текст клавиатурных поверхностей переведён с sp на dp (Р-3), панель эмодзи
  уважает «Нижний отступ» (Р-1) и сжимает свои полосы при малой высоте (Р-2);
  витринные заметки английской локали переписаны по-английски.
- `DEVICE-RESEARCH-GEOMETRY.md` — ресерч оставшихся проблем на живом устройстве
  (2026-09-04, поверх релиза 1.9.13): перебор пользовательских настроек по оси
  геометрии. Три находки — панель эмодзи игнорирует «Нижний отступ» (Р-1), при
  малой высоте клавиатуры панели не остаётся места (Р-2), полоса подсказок
  вырождается при крупном системном шрифте (Р-3). Ни одна не блокирует релиз.
- `EMOJI-PANEL-NAVBAR.md` — миссия починки Д-1 (2026-09-04): панель эмодзи
  перестала уходить под панель навигации на Android 15; перекрытие меряется
  через `getWindowVisibleDisplayFrame`, инвариант запинен `EmojiPanelStateTest`.
  Версия не бампалась.
- `TABLET-ENTER.md` — миссия починки Д-2 (2026-09-04): собственный нижний ряд
  кириллических раскладок `row_cyrillic4`, инвариант «Enter/delete/shift на
  каждом классе экранов» запинен `EnterKeyPresenceTest`. Версия не бампалась.
- `emoji-suggest/DATA.md` — миссия 1 плана эмодзи-подсказок (2026-09-01):
  курируемый ассет «слово → эмодзи» `emoji_suggest_v1.txt` (ru+tt, 681 понятие,
  3 825 записей), метод, denylist полисемии, протокол вычитки —
  `emoji-suggest/evidence-data-review.txt`.
- `emoji-suggest/ENGINE.md` — миссия 2 плана эмодзи-подсказок (2026-09-01):
  движок — эмодзи в хвостовой ячейке NEXT_WORD-полосе, append через
  `commitPredictedWord`, свой тоггл (opt-in), a11y-лейблы, эмуляторные
  свидетельства в `emoji-suggest/evidence/`.
- `emoji-suggest/FIXES.md` — миссия 4 плана эмодзи-подсказок (2026-09-02):
  три полевых дефекта — tt-покрытие поиска эмодзи (200 → 1318/1389,
  «этэч»→🐓), дефолт тоггла эмодзи-подсказок ON + конфузиблы
  («йорэк»→❤️), сброс поиска эмодзи при скрытии клавиатуры;
  до/после — `emoji-suggest/evidence/m4*.png`.
- `NEXTWORD-RACE.md` — закрытие хвоста «первый NEXT_WORD-запрос пуст»
  (2026-09-01, поверх 1.9.10): два дефекта под одним
  симптомом — страж границы кэша `extractNextWordContext` (доминирующий) и
  гонка attach E5c; свидетельства в `nextword-race/`. **Вошло в 1.9.11.**
- `SIZE-CAMPAIGN.md` — итоговый отчёт кампании ужатия (три миссии SIZE-1/2/3,
  таблица до/после, доказательства lossless, latency) — релиз 1.9.9;
  свидетельства живого обновления в `size-campaign/evidence/`.
- `SIZE-SCHEMA2.md` — миссия SIZE-1 (2026-09-01): словари переведены на TATDICT
  schema 2 (front-coding K=8 + u8-длины + varint-частоты), lossless, −198 КБ
  в словарных ассетах; дизайн, доказательство эквивалентности, latency.
- `SIZE-SCHEMA3.md` — миссия SIZE-2 (2026-09-01): таблицы биграмм переведены на
  TATBIGR schema 3 (кросс-референс в словарь: дельта-varint голов, varint-индексы
  преемников, u8-счётчики, связка со словарём по raw SHA-256), lossless, −182 КБ
  в таблицах; дизайн, эквивалентность (20 202 головы), latency.
- `RUSSIAN-BIGRAMS-REPACK.md` — перепаковка русской таблицы биграмм от текущего
  словаря (дрейф 4 195/4 195 закрыт) — 1.9.7; свидетельства в
  `russian-bigrams-repack/`.
- `CORPUS-CONVERSATIONAL-RU.md` — разговорный корпус, часть A: русская таблица
  биграмм дообучена на дедуплицированных Tatoeba + OpenSubtitles (прореживание
  1/60); 57 из 59 молчавших слов топа заговорили — вошло в 1.9.8;
  свидетельства в `corpus-conversational/`.
- `CORPUS-CONVERSATIONAL-TT.md` — разговорный корпус, часть B: татарская таблица
  дообучена на том же источнике (без прореживания — масса 3,7 %), список
  extra-heads расширен правилом 13 → 75; все шесть повелений за рангом 15 000
  заговорили — вошло в 1.9.8; свидетельства в `corpus-conversational/evidence/`
  (включая снимки полосы предсказаний релиза `release-198-*.png`).
- `restructure/evidence/` — снимки и дампы эмуляторных прогонов кампании
  (фазы 3б, 4а, 4б, 5).

## Архив

### `archive/apk-audits/` — аудиты артефактов (17)

Один отчёт на собранный APK: побайтовый размер, разрешения, прогон на
эмуляторе, гейты. Рядом — каталоги снимков `apk-audit-1.7.0/`, `apk-audit-1.8.0/`,
`apk-audit-1.8.1/`.

- `APK-AUDIT-2026-08-18.md` — аудит пересборки — 1.2.0.
- `APK-AUDIT-1.3.0.md` … `APK-AUDIT-1.9.4.md` — аудит релиза — версия в имени файла.

### `archive/dictionary/` — словари (15)

- `DICTIONARY-D0.md` — покрытие татарского словаря по Leipzig Corpora — пред-D1.
- `DICTIONARY-D1A.md` — versioned-ассет татарского top-100k — 1.3.0.
- `DICTIONARY-D1B.md` — атомарное device-protected хранилище словаря — 1.3.0.
- `DICTIONARY-D1C.md` — полоса подсказок и инсеты — 1.3.0.
- `DICTIONARY-D1D.md` — mmap prefix-движок с границами — 1.3.0.
- `DICTIONARY-D1E.md` — интеграция opt-in подсказок — 1.3.0.
- `DICTIONARY-D3.md` — автозамена с отменой — 1.3.0.
- `DICTIONARY-E4.md` — личный словарь, привязанный к активному subtype — 1.3.0.
- `RUSSIAN-DICTIONARY.md` — русский словарь top-100k, выбор словаря по раскладке — 1.7.0.
- `CORPUS.md` — разговорный регистр: кандидаты, лицензии, замер выигрыша — исследование.
- `CORPUS-OS.md` — OpenSubtitles: досчёт обоих языков, атрибуция, принятый риск — 1.9.0.
- `CORPUS-TATAR-PERMISSION-DRAFT.md` — черновик письма в corpus.tatar — не отправлено.
- `DICT-ACCEPT.md` — машинная приёмка слов из очередей — 1.9.0.
- `DICT-WIDEN.md` — расширение приёмки («всё, кроме обрывков») — 1.9.1.
- `REVIEW-BATCHES.md` — нарезка очередей слов порциями под ручную вычитку — инструмент.

Каталоги свидетельств: `dict-accept/`, `dict-widen/`, `russian-dictionary/`,
`review-batches/`; очереди и таблицы приёмки (`*-CONV-REVIEW.tsv`,
`*-QUERY-REVIEW.tsv`, `TATAR-REVIEW-QUEUE.tsv`) лежат рядом.

### `archive/bigrams/` — предсказание следующего слова (8)

- `DICTIONARY-E5A.md` — измерение биграмм, ранний гейт по размеру — 1.3.0.
- `DICTIONARY-E5B.md` — versioned-ассет татарской таблицы биграмм — 1.3.0.
- `DICTIONARY-E5C.md` — движок next-word prediction — 1.3.0.
- `DICTIONARY-E5D.md` — интеграция: контракт текста, commit path, настройки — 1.3.0.
- `RUSSIAN-BIGRAMS.md` — русская таблица биграмм, многоязычный выбор таблицы — 1.8.0.
- `BIGRAM-ADJACENCY.md` — правило соседства, отсечка K доведена до 4 — 1.8.x.
- `LANG-PRIORITY.md` — приоритет при конкуренции двух языков — 1.9.x.
- `IMPERATIVE-HEADS.md` — адресное внесение частых повелительных форм — 1.9.4.

Каталоги свидетельств: `bigram-adjacency/`, `imperative-heads/`, `lang-priority/`
(с трекнутыми `*.generated.json`-пинами), `russian-bigrams/`; отчёты-пины
`*BIGRAMS*.generated.json`, `DICTIONARY-E5A.generated.json` — рядом (не трекнуты).

### `archive/emoji/` — панель эмодзи (6)

- `DICTIONARY-E2.md` — data-ассет панели эмодзи (запись трансформации) — 1.3.0.
- `EMOJI-PANEL-REDESIGN.md` — ресерч и переделка внешнего вида панели — 1.3.0.
- `EMOJI-PANEL-TELEGRAM.md` — панель в устройстве Telegram, поиск по эмодзи — 1.5.0.
- `EMOJI-SEARCH-FIXES.md` — два дефекта поиска из 1.6.0 — 1.6.1.
- `EMOJI-SKIN-TONES.md` — кожные тона, свайп между разделами — 1.5.0.
- `SMALL-FIXES.md` — шероховатости поиска эмодзи и планшетный код — 1.8.0.

Каталог свидетельств: `emoji-panel/`.

### `archive/ux/` — эргономика и внешний вид (7)

- `FIXES-1.0.1.md` — правки по итогам первого on-device UAT — 1.0.1.
- `UX-POLISH.md` — UX-полировка, волна 1 — 1.1.0.
- `IOS-REDESIGN.md` — план iOS-редизайна интерфейса — 1.2.x.
- `LAYOUT-ERGONOMICS.md` — эргономика нижнего ряда: пробел против кнопки эмодзи — 1.4.0.
- `SYMBOL-KEY-EDGE-FIX.md` — клавиша `?123`: переключение срывалось у края — 1.4.0.
- `TOUCH-SLOP-TUNING.md` — порог касания: гистерезис и накопитель пути — 1.5.0.
- `FINAL-POLISH.md` — финальная доработка клавиатуры — 1.8.4.

Каталоги свидетельств: `final-polish/`, `layout-ergonomics/`, `symbol-key-edge/`.

### `archive/missions/` — расследования и вехи (9)

- `MILESTONE-v1.1.md` — майлстоун «Татарский первым классом» — 1.1.0.
- `MILESTONE-v2.md` — майлстоун «словарь и подсказки» — 1.3.0.
- `TAP-REPRO.md` — причина двух дефектов подсказок, пойманная падающим тестом — 1.8.1.
- `SILENT-AUDIT.md` — аудит молчаливых отказов в подсказках — 1.8.2.
- `QUARANTINE.md` — карантин личного словаря — 1.8.2.
- `PERSONAL-DICT-FIX.md` — починка молчаливых отказов личного словаря — 1.8.2.
- `PREFIX3-BUG.md` — пустая полоса подсказок на «дру» — 1.9.2.
- `SUGGEST-DIES.md` — полоса подсказок гаснет посреди слова — 1.9.3.
- `DICTIONARY-E3.md` — воспроизводимый набор опечаток, калибровка recovery@3 — 1.3.0.

Каталоги свидетельств: `prefix3/`, `suggest-dies/`; таблица
`DICTIONARY-E3-TYPO-REVIEW.tsv` — рядом.

### Прочее в архиве

- `archive/PROPOSALS.md` — замороженный (2026-08-30) архив продуктовых решений
  миссий D0–E5; живой статус — в корневом `HANDOFF.md`.
